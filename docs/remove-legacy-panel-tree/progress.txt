# Progress: Remove Legacy Panel Layout Tree (DB Nuke)

## Issue 1: Rename hierarchical types and delete legacy type definitions — DONE

**Date:** 2026-03-26

### What was done

1. Deleted the legacy `PanelLayout` interface and `PanelLayoutSchema` from `types.ts`.
2. Renamed hierarchical types to take the freed names:
   - `PanelLeafNode` -> `LeafNode`
   - `PanelSplitNode` -> `SplitNode`
   - `PanelTreeNode` -> `PanelNode`
   - `PanelLeafNodeSchema` -> `LeafNodeSchema`
   - `PanelSplitNodeSchema` -> `SplitNodeSchema`
   - `PanelTreeNodeSchema` -> `PanelNodeSchema`
3. Updated `_tag` discriminant values: `'PanelLeafNode'` -> `'LeafNode'`, `'PanelSplitNode'` -> `'SplitNode'`.
4. Updated all imports and type references across ~30 files (source + tests).
5. Kept deprecated sidebar flags (`devServerOpen`, `devServerTerminalId`, `diffOpen`) as optional fields on `LeafNode` with `@deprecated` annotations. These are needed by existing code that will be refactored in Issues 7/8. The fields are also included in `LeafNodeSchema` for backward compatibility with serialized data.
6. `PanelTab.panelLayout` type updated from `PanelTreeNode` to `PanelNode`.
7. Updated comments referencing old type names.
8. `bun run check` passes (typecheck + format + all tests across all packages).

### Files changed

- `packages/shared/src/types.ts` — Deleted legacy types section, renamed hierarchical types
- `packages/shared/test/schema.test.ts` — Updated _tag values in hierarchical fixtures
- `packages/shared/test/hierarchical-layout-types.test.ts` — Updated imports, _tag values, removed legacy test section
- `apps/web/src/panels/window-tab-utils.ts` — Updated imports and all _tag checks
- `apps/web/src/panels/layout-migration.ts` — Updated imports and _tag values
- `apps/web/src/panels/panel-tab-utils.ts` — Updated _tag value and comments
- `apps/web/src/routes/-components/workspace-frames.tsx` — Updated import and _tag check
- `apps/web/src/routes/-hooks/use-panel-layout.ts` — Updated _tag check and comments
- `apps/web/test/` — Updated 19 test files (imports, _tag values, type annotations)

## Issue 2: Schema -- single event, minimal table, delete legacy events — DONE

**Date:** 2026-03-27

### What was done

1. Deleted all 5 legacy event definitions from `schema.ts`: `layoutSplit`, `layoutPaneClosed`, `layoutPaneAssigned`, `layoutRestored`, `layoutWorkspacesReordered`.
2. Deleted all 13 hierarchical event definitions from `schema.ts`: `windowTabCreated`, `windowTabClosed`, `windowTabSwitched`, `windowTabRenamed`, `windowTabsReordered`, `panelTabCreated`, `panelTabClosed`, `panelTabSwitched`, `panelTabsReordered`, `windowLayoutRestored`, `windowLayoutSplit`, `windowLayoutPaneClosed`, `windowLayoutPaneAssigned`.
3. Deleted the intermediate schema types `layoutEventSchema` and `windowLayoutEventSchema`.
4. Deleted all 18 no-op materializers for the removed events.
5. Removed all 18 deleted events from the `events` export object.
6. Removed the `PanelNodeSchema` import from `schema.ts` (no longer needed).
7. The single `windowLayoutUpdated` event (already defined) is now the sole layout event. The `panelLayout` table already had only `windowId` and `windowLayout` columns.
8. Updated all ~30 commit sites in `use-panel-layout.ts` to use `windowLayoutUpdated` with descriptive `reason` strings instead of the old events.
9. Replaced the `commitWindowLayout` helper (which accepted an event constructor) with a version that accepts a `reason` string.
10. Replaced the `commitPanelTabLayout` helper similarly.
11. Combined dual-write patterns (legacy event + hierarchical event) into single `windowLayoutUpdated` commits.
12. Combined focus-restoration commits (window/panel tab switch + `layoutPaneAssigned`) into single commits using `saveFocusedPaneId`.
13. Extracted `commitDevServerUpdate` helper to reduce complexity in `handleToggleDevServerPane` (below lint threshold of 20).
14. Updated `schema.test.ts`: removed tests for legacy events and old hierarchical events (they no longer exist). The 6 existing `windowLayoutUpdated` tests remain and pass.
15. Updated `use-panel-layout.test.ts`: replaced all individual event mocks with a single `windowLayoutUpdatedMock`. Converted 13 tests that relied on old event payloads to `it.todo()` — these need to be rewritten with `WindowLayout` fixtures in Issues 7/9.
16. `bun run check` passes (typecheck + format + all tests across all packages).

### Files changed

- `packages/shared/src/schema.ts` — Deleted 18 events, their schemas, no-op materializers, and exports
- `packages/shared/test/schema.test.ts` — Removed legacy event and old hierarchical event tests
- `apps/web/src/routes/-hooks/use-panel-layout.ts` — Converted all ~30 commit sites to `windowLayoutUpdated`, updated helpers, extracted `commitDevServerUpdate`
- `apps/web/test/use-panel-layout.test.ts` — Replaced event mocks with `windowLayoutUpdatedMock`, converted 13 tests to `it.todo()`

## Issue 3: Reorganize utility files into 3 modules by tree level — DONE

**Date:** 2026-03-26

### What was done

1. Created `panel-tree-utils.ts` — new file containing all PanelNode-level (split tree) operations:
   - Extracted from `window-tab-utils.ts`: `splitPane` (renamed from `splitPaneInPanelTree`), `closePane` (renamed from `closePaneInPanelTree`), `findLeaf` (renamed from `findPanelTreeLeaf`), `findSiblingPaneId` (renamed from `findSiblingPaneIdInPanelTree`), `findNewLeafAfterSplit` (renamed from `findNewPanelTreeLeaf`), `getFirstLeafId` (renamed from `getFirstPanelTreeLeafId`), `getLeafIds` (renamed from `getPanelTreeLeafIds`), `collectTerminalIds` (renamed from `collectTerminalIdsFromPanelTree`), `assignTerminal` (renamed from `assignTerminalInPanelTree`), `countLeaves` (renamed from `countPanelLeaves`), `containsPane` (renamed from `panelTreeContainsPane`), `buildPath`.
   - Ported from `layout-utils.ts`: `findPaneInDirection`, `computeResize` (with `buildPath`, `getEdgeLeaf`, `tryNavigateAtAncestor`, `getResizeDelta`, `applyResizeDelta`, `computeResizeFromPath` helpers). Adapted for renamed types (`LeafNode`/`SplitNode` tags).
   - New `NavigationDirection` type exported.
2. Created `window-layout-utils.ts` — new file containing all WindowLayout-level operations:
   - Extracted all WindowLayout CRUD functions from `window-tab-utils.ts`: `addWindowTab`, `removeWindowTab`, `renameWindowTab`, `switchWindowTab`, `switchWindowTabByIndex`, `switchWindowTabRelative`, `reorderWindowTabs`.
   - Extracted workspace location lookups: `findWorkspaceLocation`, `findTerminalLocation`.
   - Extracted focus resolution: `resolveActivePaneForPanelTab`, `resolveActivePaneForWindowTab`, `saveFocusedPaneId`.
   - Extracted workspace tile leaf update: `updateWorkspaceTileLeaf`.
   - Extracted workspace uniqueness: `removeWorkspaceFromLayout`, `moveWorkspace`, `addWorkspaceToTabUnique`.
   - Extracted close confirmation: `shouldConfirmClosePanelTab`, `shouldConfirmCloseWindowTab`, `collectTerminalIdsFromTileTree`.
   - Extracted progressive close: `computeProgressiveCloseAction`.
   - Extracted terminal close: `closeTerminalInWindowLayout`.
   - Extracted utility queries: `getAllWorkspaceTileLeaves`, `getActiveWindowTab`.
   - Extracted reconciliation: `getStaleTerminalLeavesHierarchical`, `reconcileWindowLayout`.
   - Extracted repair: `repairWindowLayout` (and all helpers).
   - All type exports preserved: `ProgressiveCloseAction`, `RepairWindowLayoutResult`, `StaleTerminalLeaf`, `TerminalLocation`, `TerminalProcessInfo`, `WorkspaceLocation`.
3. `workspace-tile-utils.ts` — kept as-is (already well-organized at tile-tree level). `getWorkspaceTileLeaves` now serves as the single source of truth (duplicate in old `window-tab-utils.ts` is gone).
4. `panel-tab-utils.ts` — kept as-is (already well-organized at panel-tab level).
5. Deleted `window-tab-utils.ts` entirely (2186 lines).
6. Updated all 10 consumer files to import from the new module paths:
   - `use-panel-layout.ts` — split imports across `panel-tree-utils`, `window-layout-utils`, and `workspace-tile-utils`
   - `workspace-frames.tsx` — updated to `window-layout-utils`
   - `index.tsx` — split across `window-layout-utils` and `workspace-tile-utils`
   - `panel-hotkeys.tsx` — updated to `window-layout-utils`
   - 6 test files — updated import paths, used rename aliases where function names changed (e.g., `getFirstLeafId as getFirstPanelTreeLeafId`)
7. Updated stale `@see` comments in `panel-tab-utils.ts` and `workspace-tile-utils.ts`.
8. Created `panel-tree-utils.test.ts` with 53 tests covering all ported functions:
   - `getFirstLeafId` (3 tests), `getLeafIds` (2 tests), `findLeaf` (3 tests), `containsPane` (3 tests), `collectTerminalIds` (3 tests), `countLeaves` (2 tests), `splitPane` (4 tests), `closePane` (5 tests), `findSiblingPaneId` (5 tests), `findNewLeafAfterSplit` (2 tests), `assignTerminal` (3 tests), `findPaneInDirection` (10 tests), `computeResize` (8 tests).
9. `bun run check` passes (typecheck + format + all 1252 tests across all packages).

### Notes on deferred work

- `layout-utils.ts` is NOT deleted yet — it's still imported by production code (`use-panel-layout.ts`, `index.tsx`, `workspace-frames.tsx`, `panel-hotkeys.tsx`, `panel-content.tsx`, `workspace-frame-header-container.tsx`). These consumers will be ported in Issues 7 and 8 to use the new module hierarchy.
- `layout-migration.ts` is NOT deleted yet — it's still imported by `use-panel-layout.ts` and `workspace-frames.tsx`. These will be cleaned up in Issues 7/8/10.
- The existing `window-tab-utils.test.ts` file still exists but imports are updated to point to `window-layout-utils.ts` and `workspace-tile-utils.ts`.

### Files changed

- `apps/web/src/panels/panel-tree-utils.ts` — NEW: PanelNode-level operations (split tree)
- `apps/web/src/panels/window-layout-utils.ts` — NEW: WindowLayout-level operations
- `apps/web/src/panels/window-tab-utils.ts` — DELETED
- `apps/web/src/panels/panel-tab-utils.ts` — Updated @see comment
- `apps/web/src/panels/workspace-tile-utils.ts` — Updated @see comment
- `apps/web/src/routes/-hooks/use-panel-layout.ts` — Updated imports
- `apps/web/src/routes/-components/workspace-frames.tsx` — Updated imports
- `apps/web/src/routes/index.tsx` — Updated imports
- `apps/web/src/panels/panel-hotkeys.tsx` — Updated imports
- `apps/web/test/panel-tree-utils.test.ts` — NEW: 53 tests for panel tree operations
- `apps/web/test/window-tab-utils.test.ts` — Updated imports
- `apps/web/test/hierarchical-persistence.test.ts` — Updated imports
- `apps/web/test/workspace-uniqueness.test.ts` — Updated imports
- `apps/web/test/progressive-close.test.ts` — Updated imports
- `apps/web/test/focus-consistency.test.ts` — Updated imports
- `apps/web/test/close-confirmation.test.ts` — Updated imports

## Issue 5: PanelManager renders renamed types directly — DONE

**Date:** 2026-03-26

### What was done

1. Verified that `PanelManager` already accepts `PanelNode | undefined` and `PanelRenderer` already dispatches on `'LeafNode'` / `'SplitNode'` tags (renamed types from Issue 1). No changes needed to the rendering dispatch logic.
2. Removed the `convertPanelTreeToLegacy` call from `WorkspaceTileLeafFrame` — the active panel tab's `panelLayout` (already a `PanelNode`) is now passed directly to `WorkspaceFrame` -> `PanelManager` without any identity conversion.
3. Removed the `filterTreeByWorkspace` fallback from `WorkspaceTileLeafFrame` — pre-migration tiles (with no panel tabs) are no longer supported after the DB nuke. If no active panel tab is found, a simple empty terminal leaf is used as fallback.
4. Marked the `flatLayout` parameter on `WorkspaceTileLeafFrame` as unused (renamed to `_flatLayout`) — still in the type signature because callers pass it; will be removed from the call chain in Issues 8/10.
5. Added 12 new tests to `panel-manager.test.tsx` covering the `PanelManager` and `PanelRenderer` rendering pipeline:
   - `PanelManager`: empty state rendering, accepts `PanelNode` directly without conversion.
   - `PanelRenderer`: single `LeafNode` with terminal, `SplitNode` horizontal/vertical, nested splits, empty terminal pane CTA, `_tag` dispatch for `LeafNode` and `SplitNode`, dev server terminal pane, diff pane, review pane.
6. Preserved existing `TerminalPaneWithSidebars` tests (2 tests).
7. `LeafPaneRenderer` does not directly reference the deprecated sidebar flags (`devServerOpen`, `devServerTerminalId`, `diffOpen`) — those are consumed only by `TerminalPaneWithSidebars`, which will be simplified when dev server/diff toggles are converted to panel tabs in Issue 7.
8. `bun run check` passes (typecheck + format + all 1264 web tests, 1833 total across all packages).

### Notes on deferred work

- `convertPanelTreeToLegacy` still exists in `layout-migration.ts` and is called by `deriveLegacyTreeFromHierarchical` — will be deleted in Issue 10 (dead code removal).
- `filterTreeByWorkspace` is still imported by `workspace-frames.tsx` and used in `LegacyWorkspaceFrames` — will be removed when `LegacyWorkspaceFrames` is deleted in Issue 8.
- The `flatLayout` parameter threading through `WorkspaceFrames` -> `WorkspaceTileRenderer` -> `WorkspaceTileResizableChild` -> `WorkspaceTileLeafFrame` is now dead in the tile path but preserved for the legacy fallback in `WorkspaceFrames` — will be removed in Issue 8.

### Files changed

- `apps/web/src/routes/-components/workspace-frames.tsx` — Removed `convertPanelTreeToLegacy` call and `filterTreeByWorkspace` fallback from `WorkspaceTileLeafFrame`, pass `PanelNode` directly
- `apps/web/test/panel-manager.test.tsx` — Expanded from 2 tests to 14 tests: added `PanelManager` and `PanelRenderer` rendering tests with full mock setup

## Issue 6: Focus tracking — hierarchical only — DONE

**Date:** 2026-03-26

### What was done

1. Added `resolveActiveWorkspaceId` to `window-layout-utils.ts` — a new pure function that resolves the active workspace ID from the hierarchical `WindowLayout` by walking: active window tab > workspace tile leaves > active panel tab > focusedPaneId, and returning the `workspaceId` of the matching workspace tile leaf.
2. Changed `persistedActivePaneId` derivation in `use-panel-layout.ts` from `ensureValidActivePaneId(layout, rawPersistedActivePaneId)` (which always fell back to the first leaf since `rawPersistedActivePaneId` was hardcoded to `null`) to `resolveActivePaneForWindowTab(activeTab)` — now correctly reads `PanelTab.focusedPaneId` from the hierarchical tree.
3. Rewrote `handleSetActivePaneId` to validate against the hierarchical tree only. When `paneId` is null, falls back to `resolveActivePaneForWindowTab` instead of `ensureValidActivePaneId`. Removed dependency on the derived legacy `layout` tree.
4. Changed `activeWorkspaceId` derivation in `index.tsx` from `findNodeById(layout, activePaneId)` (legacy flat tree lookup) to `resolveActiveWorkspaceId(windowLayout)` (hierarchical resolution).
5. Removed `rawPersistedActivePaneId` constant (always `null`, no longer needed).
6. Simplified the layout repair effect — removed the `persistedActivePaneId !== rawPersistedActivePaneId` condition (which was always true and caused unnecessary repair commits). Focus repair is no longer needed since focus is tracked hierarchically within the `windowLayout` JSON.
7. Removed `ensureValidActivePaneId` from `use-panel-layout.ts` imports (no longer used in this file).
8. Added 6 tests for `resolveActiveWorkspaceId` in `focus-consistency.test.ts` covering: empty layout, single workspace, multi-workspace, no workspace layout, invalid activeTabId, empty panel tabs fallback.
9. `bun run check` passes (typecheck + format + all 1270 tests across all packages).

### Notes on deferred work

- `ensureValidActivePaneId` still exists in `layout-utils.ts` and is used by tests — will be cleaned up in Issues 8/10 when the legacy `layout` consumers are fully ported.
- The derived legacy `layout` tree (via `deriveLegacyTreeFromHierarchical`) is still computed in `use-panel-layout.ts` and used by hotkeys, panel content, and other consumers — will be removed in Issue 8.
- `findNodeById` is still used in `index.tsx` for other lookups (fullscreen, close gating, picker) — these will be ported in Issue 8.

### Files changed

- `apps/web/src/panels/window-layout-utils.ts` — Added `resolveActiveWorkspaceId` function and export
- `apps/web/src/routes/-hooks/use-panel-layout.ts` — Changed `persistedActivePaneId` to use hierarchical resolution, rewrote `handleSetActivePaneId`, removed `rawPersistedActivePaneId`, simplified repair effect
- `apps/web/src/routes/index.tsx` — Changed `activeWorkspaceId` to use `resolveActiveWorkspaceId`, added import
- `apps/web/test/focus-consistency.test.ts` — Added 6 tests for `resolveActiveWorkspaceId`

## Issue 7: Mutation handlers operate on WindowLayout directly — DONE

**Date:** 2026-03-26

### What was done

1. Rewrote all mutation handlers in `use-panel-layout.ts` to operate directly on `WindowLayout` instead of through the legacy flat `PanelNode` tree with `syncLegacyTreeToHierarchical` bridge:
   - **`handleSplitPane`**: Walks all workspace tile leaves to find the pane, calls `splitPaneInTree` on the active panel tab's `PanelNode`, updates focus to the new pane, commits `windowLayoutUpdated` with `reason: 'split'`.
   - **`handleClosePane`**: Uses extracted `closePaneInLayout` helper that finds the pane across workspaces, closes it with `closePaneInTree`, transfers focus to sibling via `findSiblingPaneId`. Removes the panel tab entirely if it was the last pane. Detects empty layouts and resets for re-seeding.
   - **`handleAssignTerminalToPane`**: Uses extracted `navigateToTerminal` for existing terminals, `ensureWorkspaceInActiveTab` to ensure workspace presence, and `assignTerminalInWorkspace` for the actual assignment (handles direct pane assignment, empty pane reuse, and auto-split).
   - **`handleCloseWorkspace`**: Walks hierarchical tree to collect terminal IDs from workspace tile leaves instead of using `getWorkspaceTerminalIds` on the legacy tree.
   - **`handleCloseTerminalPane`**: Removed legacy fast-path. Uses `closeTerminalInWindowLayout` as the sole search path across all panel tabs.
   - **`handleToggleDevServerPane`**: Completely rewritten to create/remove `devServerTerminal` panel tabs instead of toggling `devServerOpen`/`devServerTerminalId` sidebar flags on leaf nodes. Uses `findWorkspaceForPane` to locate the workspace, `addPanelTab` to create, and `removePanelTab` to remove.
   - **`handleResizePane`**: Walks workspace tile leaves to find the active panel tab containing the pane, then calls `computeResize` on that panel tab's tree.
2. Deleted `syncLegacyTreeToHierarchical` function (65 lines) — the bridge between legacy and hierarchical trees is no longer needed.
3. Deleted `commitAssignment` helper — replaced by inline `assignTerminalInWorkspace` pure function.
4. Deleted `commitDevServerUpdate` helper — replaced by panel tab operations.
5. Deleted `DEFAULT_NEW_WINDOW_LAYOUT` constant — seeding now uses `initialLayout` directly.
6. Removed `persistedLayoutTree` (always `undefined` after DB nuke), `persistedLayoutRepair`, `persistedWorkspaceOrder` (always `null`).
7. Removed `defaultLayout` (was `nativeWindowId ? DEFAULT_NEW_WINDOW_LAYOUT : initialLayout`) — simplified to use `initialLayout` directly.
8. Removed imports of `convertPanelTree`, `migrateToWindowLayout`, `filterTreeByWorkspace` from `layout-migration.ts`.
9. Removed imports of `closePane`, `splitPane`, `computeTerminalPaneAssignment`, `findLeafByTerminalId`, `getTerminalIdsToRemove`, `getWorkspaceTerminalIds`, `replaceNode`, `getStaleTerminalLeaves`, `findNewLeafAfterSplit` from `layout-utils.ts`. Only `findNodeById`, `getLeafIds`, `getLeafNodes` remain (used by the derived `layout` for Issue 8 consumers).
10. Added imports from `panel-tree-utils.ts`: `closePaneInTree`, `findLeaf`, `findSiblingPaneId`, `getPanelTreeLeafIds`, `splitPaneInTree`, `NavigationDirection` type.
11. Added import of `getAllWorkspaceTileLeaves` from `window-layout-utils.ts`.
12. Extracted 5 pure helper functions at module level to reduce cognitive complexity below lint threshold (max 20):
    - `closePaneInLayout` — finds and closes a pane across all workspace tile leaves
    - `navigateToTerminal` — switches to the tab/panel tab containing a terminal
    - `findWorkspaceForPane` — locates the workspace tile leaf containing a pane
    - `ensureWorkspaceInActiveTab` — ensures a workspace exists in the active window tab
    - `assignTerminalInWorkspace` — assigns a terminal to a workspace's active panel tab (handles direct, empty, and split cases)
    - `findEmptyTerminalPaneInTree` — finds an unoccupied terminal pane in a panel tree
13. Kept `deriveLegacyTreeFromHierarchical` for the `layout` derivation — consumers (`PanelHotkeys`, `PanelContent`, `WorkspaceFrames`, `index.tsx`) still depend on the flat tree. Will be ported in Issue 8.
14. Kept `layout-utils.ts` imports for `findNodeById`, `getLeafIds`, `getLeafNodes` — used by the derived `layout` value passed to consumers. Will be removed in Issue 8.
15. `bun run check` passes (typecheck + format + all 1270 web tests, all packages pass).

### Notes on deferred work

- `layout-migration.ts` still has `deriveLegacyTreeFromHierarchical` used by the `layout` derivation — will be removed in Issue 8 when consumers are ported.
- `layout-utils.ts` still has `findNodeById`, `getLeafIds`, `getLeafNodes` used by the derived `layout` value — will be removed in Issue 8.
- The 13 `it.todo()` tests in `use-panel-layout.test.ts` from Issue 2 still need to be rewritten with `WindowLayout` fixtures — these are for Issues 8/9.
- The deprecated sidebar flags (`devServerOpen`, `devServerTerminalId`, `diffOpen`) on `LeafNode` are no longer written by mutation handlers but may still be read by rendering code (`TerminalPaneWithSidebars`). Full removal is in Issue 8.

### Files changed

- `apps/web/src/routes/-hooks/use-panel-layout.ts` — Rewrote all mutation handlers, deleted `syncLegacyTreeToHierarchical`, `commitAssignment`, `commitDevServerUpdate`, `DEFAULT_NEW_WINDOW_LAYOUT`, `persistedLayoutTree`, simplified imports

