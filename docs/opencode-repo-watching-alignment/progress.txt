2026-03-06
- Selected Issue 2 as the highest-priority remaining slice because the watcher backend is the main parity gap called out in the PRD and it unblocks the later ignore/event-semantic work.
- Replaced the default `FileWatcher` implementation with a native `@parcel/watcher` backend while preserving the existing service contract and coordinator wiring.
- Added an explicit `fs.watch` fallback path that can be selected without changing coordinator code, and automatically falls back with a warning if the native backend cannot start.
- Added focused watcher tests covering default native preference, native subscribe/close behavior, native-to-fallback failover, and explicit fallback selection.
- Feedback loops: `bun run test -- test/file-watcher.test.ts` passes in `packages/server`; `bun run check-types` still fails because of pre-existing `ProjectRecord` fixture mismatches in `packages/server/test/prd-rpc-handlers.test.ts` and `packages/server/test/project-rpc-handlers.test.ts`.

2026-03-06
- Selected Issue 1 as the highest-priority remaining slice because persisted repository identity is the next foundational dependency in the PRD and it unblocks startup-restore work.
- Extended project identity persistence so new project writes include `repoId` and `canonicalGitCommonDir`, and legacy project reads now lazily backfill those fields through `ProjectRegistry`.
- Updated startup watcher restore to reuse persisted canonical git identity when available instead of re-resolving everything from raw repo paths.
- Added regression coverage for new-project persistence, lazy legacy backfill, persisted-identity startup restore, and updated RPC fixture types to match the expanded project record shape.
- Feedback loops: `bun run check-types` passes in `packages/server` and `packages/shared`; `bun run test -- test/repository-identity.test.ts test/repository-watch-coordinator-hardening.test.ts test/rpc/project-management.test.ts test/project-rpc-handlers.test.ts test/prd-rpc-handlers.test.ts` passes in `packages/server`; `bun run test -- test/schema.test.ts` passes in `packages/shared`; `bun run check` still fails on a pre-existing formatting issue in `.opencode/package.json`.

2026-03-06
- Selected Issue 3 as the highest-priority remaining slice because it unblocks Issues 6 and 7 and directly addresses a core PRD concern about filtering noise earlier in the pipeline.
- Added `watchIgnore` field to `LaborerConfig` and `ResolvedLaborerConfig` so ignore patterns can be extended via `laborer.json` without code changes.
- Extended `FileWatcher.subscribe` options to accept `ignore` glob patterns; the native `@parcel/watcher` backend now passes these to the underlying watcher for early suppression.
- Added `mergeIgnorePrefixes`, `toWatcherIgnoreGlobs`, and `setAdditionalIgnorePrefixes` to `RepositoryEventBus` so the coordinator can wire config-provided patterns into the centralized ignore model.
- Updated `RepositoryWatchCoordinator` to resolve project config at watch setup time, merge additional ignore prefixes into the event bus, and pass derived ignore globs to the repo-root watcher subscription.
- Added `ConfigService` as a dependency of `RepositoryWatchCoordinator.layer` and updated all test layers (13 sites across 8 test files plus `main.ts` and `test/rpc/test-layer.ts`) to provide `ConfigService`.
- Added unit tests for `mergeIgnorePrefixes`, `toWatcherIgnoreGlobs`, `setAdditionalIgnorePrefixes`, and config-driven ignore suppression through the full watcher-to-event-bus pipeline.
- Added integration tests for custom `watchIgnore` patterns from `laborer.json` and build output churn suppression across default ignore rules.
- Feedback loops: `bun run check-types` passes in `packages/server` and `packages/shared`; all affected test suites pass (repository-event-bus, repository-watch-coordinator, coordinator-hardening, branch-state-tracker, startup-bootstrap, worktree-watcher, project-registry.worktree-detection, repository-identity, config-service, file-watcher, health-check, prd-rpc-handlers, project-rpc-handlers); `bun run check` still fails on a pre-existing formatting issue in `.opencode/package.json`.

2026-03-06
- Selected Issue 4 as the highest-priority remaining slice because it unblocks Issue 6 (downstream consumers) and Issue 7 (parity hardening), while Issue 5 (startup restore) does not unblock further work.
- Extended `WatchEvent` with an optional `nativeKind` field (`"create" | "update" | "delete"`) that the native `@parcel/watcher` backend populates directly from the backend's authoritative event classification. The `fs.watch` fallback leaves `nativeKind` undefined.
- Updated the native watcher backend to propagate `event.type` from `@parcel/watcher` as `nativeKind` on every emitted `WatchEvent`, preserving the legacy `type` field for backward compatibility.
- Extracted a `resolveEventType` helper in `RepositoryWatchCoordinator` that prefers `nativeKind` when present (direct mapping: create→add, update→change, delete→delete) and falls back to the legacy `existsSync` inference only when `nativeKind` is absent.
- Updated the `RepositoryFileEventType` doc comment to reflect that event classification is now authoritative when the native backend is active, and best-effort only under the `fs.watch` fallback.
- Added 6 new integration tests covering: native create→add mapping, native update→change mapping, native delete→delete mapping (including the case where the file still exists on disk), fallback inference via existsSync, rename-heavy churn with native backend producing accurate classifications, and verification that event fanout remains backend-agnostic (no `nativeKind` leaking to downstream `RepositoryFileEvent`).
- Feedback loops: `bun run check-types` passes in `packages/server` and `packages/shared`; all affected test suites pass (repository-event-bus 34 tests, file-watcher 4 tests, repository-watch-coordinator 5 tests, repository-watch-coordinator-hardening 8 tests, repository-identity 13 tests, prd-rpc-handlers 20 tests, project-rpc-handlers 1 test).

2026-03-06
- Selected Issue 5 as the highest-priority remaining slice because it is unblocked (Issue 1 is complete), foundational for the final hardening pass (Issue 7), and ensures the full project lifecycle uses persisted identity end-to-end.
- Updated `RepositoryWatchCoordinator.watchProject` to accept an optional pre-resolved `canonicalGitCommonDir` parameter so callers that already have identity (e.g. `ProjectRegistry.addProject`) skip redundant git re-resolution.
- Updated `ProjectRegistry.addProject` to pass the already-resolved `canonicalGitCommonDir` and project name directly to the coordinator, eliminating the double identity resolution that previously occurred on every project add.
- The `watchAll` startup path already used persisted `canonicalGitCommonDir` (from Issue 1); legacy projects without identity fields continue to fall through to the re-resolution path via `watchProject`.
- Added integration test "server boot restores watchers for projects with persisted identity without re-resolving" — seeds a project with fully-populated `repoId` and `canonicalGitCommonDir`, builds the coordinator layer, and verifies worktree reconciliation and watcher operation without identity re-resolution.
- Added integration test "startup restore produces the same workspace and branch state as fresh registration" — registers a project via `addProject`, captures workspace/branch state, removes it, re-seeds with persisted identity, builds the coordinator for a simulated restart, and asserts that the restored state matches the fresh registration state exactly.
- Feedback loops: `bun run check-types` passes in `packages/server` and `packages/shared`; all affected test suites pass (startup-bootstrap 8 tests, repository-watch-coordinator 5 tests, repository-watch-coordinator-hardening 8 tests, repository-event-bus 34 tests, repository-identity 13 tests, worktree-watcher 4 tests, prd-rpc-handlers 20 tests, project-rpc-handlers 1 test, rpc/project-management 6 tests).

2026-03-06
- Selected Issue 6 as the highest-priority remaining slice because it is the only unblocked pending issue (blocked by #3 and #4, both complete), and it must be completed before the final hardening pass (Issue 7).
- Wired `DiffService` as the first production downstream consumer of the `RepositoryEventBus`. The service now subscribes to the event bus during layer construction and triggers debounced diff refreshes for all actively-polled workspaces when file events arrive for their project.
- Changed `DiffService.layer` from `Layer.effect` to `Layer.scoped` so the event bus subscription and debounce timers are cleaned up on scope close via `Effect.addFinalizer`.
- Added a per-project debounce timer (300ms) to coalesce rapid file events (e.g. branch switch, dependency install) before invoking `getDiff`, preventing git from being overwhelmed under heavy churn.
- The event-driven path supplements polling — it does not replace it — so diffs remain fresh even when the watcher is temporarily degraded.
- The `DiffService` does not own any watchers itself; it consumes normalized `RepositoryFileEvent` objects from the bus, keeping it backend-agnostic and avoiding duplicate watcher ownership.
- Added 4 integration tests covering: event-driven refresh attempt for actively-polled workspaces (verifies bus subscription fires and errors are non-fatal), harmless handling of events for projects without polling, multiple concurrent consumers alongside DiffService, and debounce coalescing of rapid file event bursts.
- Feedback loops: `bun run check-types` passes in `packages/server` and `packages/shared`; `bun x ultracite check` passes; all affected test suites pass (diff-service-event-consumer 4 tests, repository-event-bus 34 tests, repository-watch-coordinator 5 tests, repository-watch-coordinator-hardening 8 tests, repository-identity 13 tests, file-watcher 4 tests, startup-bootstrap 8 tests, branch-state-tracker 9 tests, worktree-watcher 4 tests, prd-rpc-handlers 20 tests, project-rpc-handlers 1 test, rpc/project-management 6 tests).
