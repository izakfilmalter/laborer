## Issue 1: Review pane type + panel system wiring — DONE

Completed all acceptance criteria:

- Added 'review' to PaneType Schema.Literal in packages/shared/src/types.ts
- Added 'review' to VALID_PANE_TYPES in apps/web/src/panels/layout-utils.ts
- Created placeholder ReviewPane component in apps/web/src/panes/review-pane.tsx
  (renders heading with ClipboardCheck icon and workspace ID)
- Added routing branch in PaneContent (panel-manager.tsx) for paneType === 'review'
- Added toggleReviewPane action to PanelActions interface (panel-context.tsx)
  - Splits right from active pane with paneType: 'review', inheriting workspaceId
  - Toggles off by finding and closing existing review pane for the workspace
- Implemented handleToggleReviewPane in use-panel-layout.ts
- Added 6 tests in layout-utils.test.ts covering:
  - repairPanelLayoutTree preserves valid review leaves
  - repairPanelLayoutTree preserves review panes in splits
  - splitPane creates review pane with newPaneContent
  - split-right creates exactly one new leaf
  - findNewLeafAfterSplit finds the review pane
  - closePane removes review pane and collapses split
- Updated mock PanelActions in 3 test files to include toggleReviewPane
- All 387 tests pass, TypeScript clean, formatting clean

## Issue 2: PR comment fetching RPC + server service — DONE

Completed all acceptance criteria:

- Defined shared response schemas in packages/shared/src/rpc.ts:
  - PrCommentReaction (id, content, userId)
  - PrComment (id, commentType, authorLogin, authorAvatarUrl, body, filePath,
    line, createdAt, reactions)
  - ReviewFetchCommentsResponse (comments array)
- Added review.fetchComments RPC to LaborerRpcs group with workspaceId payload
- Created ReviewCommentFetcher server service (packages/server/src/services/review-comment-fetcher.ts):
  - Context.Tag + Layer.effect pattern matching existing services
  - fetchComments method resolves workspace from LiveStore, detects PR number
    (cached or fallback to gh pr view), detects owner/repo from git remote
  - Fetches issue comments via gh api repos/{owner}/{repo}/issues/{pr}/comments --paginate
  - Fetches inline review comments via gh api repos/{owner}/{repo}/pulls/{pr}/comments --paginate
  - Fetches reactions for each comment in parallel (concurrency: 5)
  - Returns typed PrComment[] with commentType, author info, body, file/line, reactions
  - Handles paginated gh output (concatenated JSON arrays)
  - Actionable error messages for auth failures, rate limits, missing PR
- Reuses parseGithubRepo from GithubTaskImporter for SSH/HTTPS remote URL parsing
- Added review.fetchComments handler in handlers.ts delegating to ReviewCommentFetcher
- Wired ReviewCommentFetcher.layer into main.ts and test-layer.ts
- Added 6 tests in review-comment-fetcher.test.ts covering:
  - Fetches and maps issue + review comments with reactions
  - Parses owner/repo from SSH remote URL (git@github.com:...)
  - Parses owner/repo from HTTPS remote URL (https://github.com/...)
  - Returns PR_NOT_FOUND when no PR exists and prNumber not cached
  - Returns NOT_FOUND when workspace does not exist
  - Handles gh auth failure with actionable error message
- All 269 tests pass, TypeScript clean, formatting clean

## Issue 4: Finding extraction (server-side parsing) — DONE

Completed all acceptance criteria:

- Defined new shared schemas in packages/shared/src/rpc.ts:
  - ReviewSeverity (literal: 'critical' | 'warning' | 'info')
  - ReviewFinding (id, file, line, severity, description, suggestedFixes,
    category, dependsOn, commentId, reactions)
  - ReviewVerdict (literal: 'approved' | 'needs_fix')
- Updated ReviewFetchCommentsResponse to new shape:
  { verdict: ReviewVerdict | null, findings: ReviewFinding[], comments: PrComment[] }
- Implemented finding extraction in review-comment-fetcher.ts:
  - Parses `<!-- brrr-finding:{json} -->` markers from inline review comment bodies
  - Extracts all ReviewFinding fields including suggestedFixes, category, dependsOn
  - Handles brrr's `\u002d\u002d` escaping for `--` in HTML comments
  - Comments with valid finding markers go to `findings` array
  - Comments with malformed JSON fall back to `comments` array gracefully
  - Missing markers produce empty `findings` array
- Implemented verdict extraction:
  - Detects `<!-- brrr-review -->` marker in issue comments
  - Parses verdict from rendered text: `- Verdict: ✅ \`approved\`` / `- Verdict: ❌ \`needs_fix\``
  - Returns null verdict when no summary comment exists
  - Summary comment still included in `comments` array for context
- Added 7 new tests in review-comment-fetcher.test.ts covering:
  - Valid finding marker parsing with all fields (severity, suggestedFixes, etc.)
  - Malformed JSON falls back to comments array gracefully
  - Multiple findings across multiple review comments (incl. null depends_on normalization)
  - Approved verdict extraction from summary comment
  - needs_fix verdict extraction from summary comment
  - Null verdict when no brrr-review comment exists
  - Mixed comments: findings separated from human comments, verdict extracted
- All 276 tests pass (269 + 7 new), TypeScript clean, formatting clean

## Issue 3: Review pane renders fetched comments — DONE

Completed all acceptance criteria:

- Replaced placeholder ReviewPane component with data-fetching implementation
  in apps/web/src/panes/review-pane.tsx
- Uses LaborerClient.query('review.fetchComments', { workspaceId }) with
  useMemo for reactive data fetching on mount
- Inner ReviewPaneContent component wrapped in Suspense for loading state
- Loading state: Spinner with "Loading comments..." text (both Suspense
  fallback and Initial/waiting state)
- Empty state (no PR): detects PR_NOT_FOUND error code via extractErrorCode,
  shows GitPullRequestClosed icon with guidance to create a PR
- Error state: Alert with destructive variant showing error message
- Empty state (PR exists, no comments): ClipboardCheck icon with guidance
- Comment rendering via CommentCard component:
  - Avatar with author image and 2-letter fallback
  - Author login name and formatted timestamp
  - File path and line number reference for inline review comments
    (FileCode icon, shows file:line or just file when line is null)
  - Full comment body with whitespace preservation
- ScrollArea wrapping the comment list for overflow handling
- Added 11 tests in apps/web/test/review-pane.test.tsx covering:
  - Renders loading state during initial fetch
  - Renders loading state when waiting for data
  - Renders empty state when no PR exists (PR_NOT_FOUND)
  - Renders error state when RPC fails
  - Renders comments with author info and body
  - Renders inline review comments with file path and line number
  - Renders file path without line number when line is null
  - Does not show file reference for issue comments
  - Renders multiple comments
  - Renders empty state when PR has no comments or findings
  - Renders the review header bar
- All 398 tests pass, TypeScript clean, formatting clean

## Issue 5: Grouped display with severity badges — DONE

Completed all acceptance criteria:

- Replaced flat comment list with two grouped sections: "Findings" and "Comments"
- Each section uses a collapsible ReviewSection component with:
  - ChevronRight toggle icon with rotate-90 transition (matching project-group pattern)
  - Section icon (Search for Findings, MessageSquare for Comments)
  - Section heading and count Badge
- FindingCard component renders structured finding data:
  - SeverityBadge with data-severity attribute and brrr-convention colors:
    red (bg-red-500/10 text-red-600) for critical,
    yellow (bg-yellow-500/10 text-yellow-700) for warning,
    blue (bg-blue-500/10 text-blue-600) for info
  - Category tag as secondary Badge (hidden when category is null)
  - File:line reference with FileCode icon
  - Description text with whitespace preservation
  - Collapsible suggested fixes using Collapsible component
    (collapsed by default, shows "Suggested fix(es) (N)" trigger)
- Findings sorted by severity: critical first, then warning, then info
  via SEVERITY_ORDER lookup and Array.sort
- CommentCard retained from Issue 3 (author avatar, login, timestamp,
  file:line for inline comments, body)
- Sections conditionally rendered: Findings section hidden when no findings,
  Comments section hidden when no comments
- Added 12 new tests in review-pane.test.tsx (23 total) covering:
  - Findings render with correct severity badges
  - Finding cards show file:line reference, category, and description
  - Findings and Comments appear as separate grouped sections
  - Section counts displayed in badges
  - Findings sorted by severity (critical > warning > info)
  - Collapsible suggested fixes expand on click
  - No suggested fixes trigger when finding has no fixes
  - Findings section hidden when no findings
  - Comments section hidden when no comments
  - Finding without category renders without category badge
  - Severity badges have correct data-severity attributes
  - Comments render with author info in Comments section
- All 410 tests pass (web: 410, server: 276), TypeScript clean, formatting clean

## Issue 6: Polling + manual refresh — DONE

Completed all acceptance criteria:

- Added 30-second polling interval via useEffect + setInterval calling
  useAtomRefresh from @effect-atom/atom-react/Hooks
- Polling starts automatically on mount and stops cleanly on unmount
  (clearInterval in useEffect cleanup, intervalRef nulled)
- Manual refresh button (RefreshCw icon, ghost variant, aria-label
  "Refresh comments") triggers immediate fetch and resets the polling
  timer so the next automatic poll is always 30s after the last action
- Subtle refresh indicator ("Refreshing..." with spinning RefreshCw icon)
  shown during background refetch when existing data is present
  (result._tag === 'Success' && result.waiting) — does NOT replace
  existing content, avoiding layout shifts and content flicker
- Initial loading state still shows full-screen spinner (no data yet)
- ReviewHeaderBar component extracted: renders refresh indicator and
  manual refresh button, included in all content states (loading,
  error, empty, populated) so refresh is always accessible
- POLL_INTERVAL_MS constant (30_000) defined at module scope
- Implementation uses useCallback for stable function references and
  useRef for the interval ID to avoid stale closures
- Added 8 new tests in review-pane.test.tsx (31 total) covering:
  - Polling starts on mount and calls refresh after 30 seconds
  - Polling stops when the pane is unmounted (no leaked intervals)
  - Renders the refresh button
  - Manual refresh button triggers immediate fetch
  - Manual refresh resets the polling timer (verified with fake timers)
  - Subtle refresh indicator shown when refetching with existing data
  - Refresh indicator hidden when not refetching
  - Refresh button renders in all content states (error, no-PR)
- All 418 tests pass (web: 418, server: 276), TypeScript clean, formatting clean

## Issue 7: Verdict badge on workspace card — DONE

Completed all acceptance criteria:

- Defined new `review.fetchVerdict` RPC in packages/shared/src/rpc.ts:
  - ReviewFetchVerdictResponse schema with `verdict: Schema.NullOr(ReviewVerdict)`
  - Lightweight RPC with workspaceId payload, returns only verdict
- Added `fetchVerdict` method to ReviewCommentFetcher service
  (packages/server/src/services/review-comment-fetcher.ts):
  - Reuses workspace resolution, PR detection, and owner/repo detection
  - Fetches only issue comments (skips inline review comments and reactions)
  - Extracts verdict from `<!-- brrr-review -->` marker using existing
    isBrrrReviewComment + extractVerdict helpers
  - Much lighter than fetchComments — single API call vs 3+ calls
- Added `review.fetchVerdict` handler in packages/server/src/rpc/handlers.ts
  delegating to ReviewCommentFetcher.fetchVerdict
- Created ReviewVerdictBadge component
  (apps/web/src/components/review-verdict-badge.tsx):
  - Self-manages data fetching via LaborerClient.query + useAtomValue
  - Returns null when loading, on error, or when verdict is null (no review)
  - Shows green checkmark icon (Check) for "approved" verdict with
    border-success/30 bg-success/10 text-success styling
  - Shows red X icon (X) for "needs_fix" verdict with
    border-destructive/30 bg-destructive/10 text-destructive styling
  - Compact badge (icon only) wrapped in Tooltip showing "Review: approved"
    or "Review: needs fix"
  - data-testid="review-verdict-badge" and data-verdict attribute for testing
  - Follows GitHubPrStatusBadge pattern (cn(), Tooltip, lucide icons)
- Integrated into workspace card (apps/web/src/components/workspace-list.tsx):
  - ReviewVerdictBadge renders next to GitHubPrStatusBadge in the card header
  - Only rendered when workspace.prNumber is non-null (PR exists)
  - Wrapped in Suspense with null fallback so loading doesn't block the card
- Added 6 server tests in review-comment-fetcher.test.ts covering:
  - Returns approved verdict from brrr-review summary comment
  - Returns needs_fix verdict from brrr-review summary comment
  - Returns null verdict when no brrr-review comment exists
  - Returns null verdict when issue comments are empty
  - Does not fetch inline review comments or reactions (lightweight check)
  - Returns error when workspace does not exist
- Added 10 UI tests in apps/web/test/review-verdict-badge.test.tsx covering:
  - Renders nothing during initial loading
  - Renders nothing when the fetch fails
  - Renders nothing when verdict is null (no review exists)
  - Renders approved badge with correct data-verdict attribute
  - Renders needs_fix badge with correct data-verdict attribute
  - Renders approved badge with green success styling
  - Renders needs_fix badge with red destructive styling
  - Renders tooltip with "Review: approved" for approved verdict
  - Renders tooltip with "Review: needs fix" for needs_fix verdict
  - Renders badge as compact (icon only, no text)
- All 710 tests pass (web: 428, server: 282), TypeScript clean, formatting clean
