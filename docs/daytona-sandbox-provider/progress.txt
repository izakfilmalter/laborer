## Issue 1: Schema rename — container* columns to sandbox*

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Renamed 6 workspace table columns in packages/shared/src/schema.ts:
  containerId -> sandboxId, containerUrl -> sandboxUrl, containerPort -> sandboxPort,
  containerImage -> sandboxImage, containerStatus -> sandboxStatus, containerSetupStep -> sandboxSetupStep
- Added new sandboxProvider column (text, nullable) to workspaces table
- Updated WorkspaceCreated materializer to initialize all sandbox* columns to null + sandboxProvider to null
- Updated all v1.Container* event materializers to write to sandbox* columns (event field names preserved for backward compat)
- Old v1.Container* event definitions retained (not deleted) for LiveStore backward compatibility
- Updated server-side code: container-service.ts, workspace-provider.ts, terminal-client.ts, rpc/handlers.ts
- Updated web UI code: workspace-dashboard.tsx, workspace-list.tsx, workspace-frame-header-container.tsx, use-panel-layout.ts
- Updated all tests: schema.test.ts, container-service.e2e.test.ts, workspace-management.test.ts,
  workspace-card-layout.test.tsx, workspace-plan-scope.test.tsx, workspace-frame-header-container.test.tsx,
  workspace-list-root-delete.test.tsx, first-launch-empty-cache.test.tsx

### Verification:
- All 15 turbo tasks pass (typecheck + format + test across all packages)
- 1070 web tests pass, 410 server tests pass, 81 shared tests pass

## Issue 2: Schema — new v2.Sandbox* events + bridging materializers

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Defined 6 new v2.Sandbox* events in packages/shared/src/schema.ts:
  v2.SandboxStarted (workspaceId, sandboxId, sandboxUrl, sandboxImage, sandboxPort?, sandboxProvider)
  v2.SandboxStopped (workspaceId)
  v2.SandboxPaused (workspaceId)
  v2.SandboxResumed (workspaceId)
  v2.SandboxSetupStepChanged (workspaceId, step)
  v2.SandboxPortChanged (workspaceId, sandboxPort)
- Added materializers for all 6 new events writing to sandbox* columns
- v2.SandboxStarted materializer sets sandboxProvider column (v1 events do not)
- All new events added to the `events` export object
- Old v1.Container* events and materializers remain unchanged for backward compatibility
- Both v1 and v2 events coexist in the schema

### Tests added (8 new tests in packages/shared/test/schema.test.ts):
- v2.SandboxStarted sets sandbox fields and sandboxProvider
- v2.SandboxStarted works without optional sandboxPort
- v2.SandboxStopped clears sandbox state
- v2.SandboxPaused sets sandboxStatus to paused
- v2.SandboxResumed sets sandboxStatus to running
- v2.SandboxSetupStepChanged updates sandboxSetupStep (including null clear)
- v2.SandboxPortChanged updates sandboxPort (including null clear)
- v2.Sandbox* and v1.Container* events coexist and both materialize correctly

### Verification:
- All 15 turbo tasks pass (typecheck + format + test across all packages)
- 89 shared tests pass (22 schema tests), 410 server tests pass

## Issue 8: SandboxProvider Effect interface definition

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Created new file packages/server/src/services/sandbox-provider.ts
- Defined SandboxProvider Effect Context.Tag with identifier '@laborer/SandboxProvider'
- Interface includes 8 methods:
  createSandbox(params: CreateSandboxParams) => Effect<void, RpcError>
  destroySandbox(workspaceId: string) => Effect<void, RpcError>
  pauseSandbox(workspaceId: string) => Effect<void, RpcError>
  resumeSandbox(workspaceId: string) => Effect<void, RpcError>
  getPreviewUrl(workspaceId: string, port: number) => Effect<string, RpcError>
  spawnTerminal(workspaceId: string, opts?: TerminalOpts) => Effect<TerminalHandle, RpcError>
  reconcileState() => Effect<void>
  checkAvailability() => Effect<ProviderStatus>
- Defined CreateSandboxParams type with workspaceId, branchName, projectName, worktreePath,
  devServerConfig (matching ResolvedDevServerConfig shape), and optional onReady callback
- Defined TerminalOpts type with optional command, autoRun, cols, rows
- Defined TerminalHandle type with id, workspaceId, command, status — matches existing
  TerminalRecord shape from terminal-client.ts for drop-in compatibility
- Defined ProviderStatus type with available boolean + optional error string — matches
  existing DockerStatus shape from docker-detection.ts
- All types exported for use by both Docker and Daytona implementations

### Design decisions:
- CreateSandboxParams.devServerConfig uses concrete types (not ResolvedValue<T> wrappers)
  since providers need the resolved values, not the provenance metadata
- TerminalHandle intentionally matches TerminalRecord to minimize adapter code in Issue 9
- ProviderStatus intentionally matches DockerStatus to minimize adapter code in Issue 9
- No layer defined on SandboxProvider — implementations provide their own layers
- reconcileState and checkAvailability have no error channel (Effect<void> / Effect<ProviderStatus>)
  since failures are handled internally (logged, not propagated)

### Verification:
- All turbo typecheck tasks pass (7/7)
- bun run format passes

## Issue 9: DockerSandboxProvider — wrap existing ContainerService behind SandboxProvider

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Created new file packages/server/src/services/docker-sandbox-provider.ts
- Defined DockerSandboxProvider as a Context.Tag that produces a SandboxProvider value
- DockerSandboxProvider.layer depends on ContainerService, DepsImageService, DockerDetection,
  TerminalClient, and LaborerStore
- All 8 SandboxProvider interface methods implemented as thin delegation:
  - createSandbox: orchestrates DepsImageService.ensureDepsImage + ContainerService.createContainer
    (mirrors existing performContainerSetup flow from WorkspaceProvider)
  - destroySandbox: delegates to ContainerService.destroyContainer
  - pauseSandbox: delegates to ContainerService.pauseContainer
  - resumeSandbox: delegates to ContainerService.unpauseContainer
  - getPreviewUrl: constructs http://{sandboxUrl}:{port} from LiveStore workspace data,
    falls back to containerName() utility for URL derivation
  - spawnTerminal: delegates to TerminalClient.spawnInWorkspace (which already handles
    docker exec path for containerized workspaces)
  - reconcileState: no-op — ContainerService.layer already runs startup reconciliation
    + docker events listener as daemon fibers
  - checkAvailability: delegates to DockerDetection.check()

### Design decisions:
- Pure adapter layer: no new behavior introduced, existing Docker flow unchanged
- ContainerService continues to emit v1.Container* events internally (both v1 and v2
  materializers write to the same sandbox* columns, so this is correct)
- Full migration of Docker path to v2 events deferred to Issue 10 (WorkspaceProvider refactor)
- Uses Layer.effect (not Layer.scoped) since no finalizers are needed — cleanup is handled
  by the underlying services (ContainerService, TerminalClient)
- createSandbox looks up project repoPath from LiveStore for lockfile detection, falling
  back to worktreePath if project lookup fails
- getPreviewUrl returns full http:// URL with port appended

### Verification:
- All turbo typecheck tasks pass (server, shared, file-watcher)
- bun run format passes (487 files, no fixes)
- 410 server tests pass (48 test files)
- 89 shared tests pass

## Issue 10: WorkspaceProvider refactor — delegate to SandboxProvider instead of ContainerService

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Refactored WorkspaceProvider.layer to depend on SandboxProvider instead of ContainerService + DepsImageService
- Removed ContainerService and DepsImageService imports from workspace-provider.ts
- Added SandboxProvider import and dependency
- Renamed `performContainerSetup` to `performSandboxSetup` — now delegates to
  `sandboxProvider.createSandbox()` instead of directly calling depsImageService + containerService
- Renamed `startContainer` to `startSandbox` in the WorkspaceProvider service interface
- Updated `destroyWorktree` background cleanup: replaced direct containerService.destroyContainer
  + deterministic name fallback with `sandboxProvider.destroySandbox()`
- Removed Docker-specific helpers (`dockerContainerExists`, `destroyContainerByName`) from
  workspace-provider.ts — these are now in docker-sandbox-provider.ts
- Updated error handler to use `sandboxSetupStepChanged` instead of `containerSetupStepChanged`
- Updated CreateSandboxParams.onReady type to `| undefined` for exactOptionalPropertyTypes compat

### Handler updates (rpc/handlers.ts):
- `workspace.startContainer` handler now calls `provider.startSandbox()` instead of `provider.startContainer()`
- `container.unpause` fallback handler updated to call `provider.startSandbox()`

### DockerSandboxProvider enhancements (docker-sandbox-provider.ts):
- Enhanced `destroySandbox` to handle deterministic name-based container cleanup (moved from
  WorkspaceProvider) — checks if workspace has sandboxId, falls back to containerName() for
  leaked container cleanup when origin is 'laborer'
- Added `dockerContainerExists` and `destroyContainerByName` helpers (moved from workspace-provider.ts)

### Layer composition updates:
- utility-main.ts: Added `DockerSandboxProvider.layer` to DeferredServiceStack via `Layer.provide()`
  and provided TerminalClient proxy to the stack build
- test/rpc/test-layer.ts: Added DockerSandboxProvider.layer to DeferredServiceStack, created
  `DeferredServiceStackWithTerminal` to provide TestTerminalClient to the stack
- test/workspace-destroy-origin.test.ts: Updated TestLayer to include DockerSandboxProvider.layer,
  DockerDetection.layer, and a stub TestTerminalClient

### Verification:
- All 7 turbo typecheck tasks pass
- bun run format passes (487 files, no fixes)
- 410 server tests pass (48 test files)
- 89 shared tests pass

## Issue 7: Daytona env vars in packages/env

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Extended packages/env/src/server.ts with three new Daytona-specific env vars:
  - DAYTONA_API_KEY: optional string (z.string().min(1).optional()) — only required at
    runtime when Daytona provider is actually used, validated by DaytonaClient service
  - DAYTONA_API_URL: optional string with URL validation, defaults to "https://app.daytona.io/api"
  - DAYTONA_TARGET: optional string, defaults to "us"
- All three vars use Zod validation consistent with existing env vars (TERMINAL_GRACE_PERIOD_MS, etc.)
- Variables are loaded via @t3-oss/env-core with emptyStringAsUndefined: true
- .env.local already contains DAYTONA_API_KEY and DAYTONA_API_URL values — now properly
  validated and typed through the env schema

### Design decisions:
- DAYTONA_API_KEY is optional at the schema level (not required) because not every user
  has Daytona credentials. Runtime validation happens in the DaytonaClient service (Issue 11)
  when the provider is actually used
- DAYTONA_API_URL uses z.string().url() for proper URL validation with a sensible default
- DAYTONA_TARGET uses z.string().min(1) (not an enum) to allow future region values without
  schema changes
- No test files added — packages/env has no existing test infrastructure, and env validation
  is tested implicitly by the server test suite which imports the env module

### Verification:
- All 15 turbo tasks pass (typecheck + format + test across all packages)
- 410 server tests pass (48 test files)
- 89 shared tests pass
- bun run format passes (487 files, no fixes)

## Issue 11: Daytona SDK client Effect service (thin wrapper)

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Installed `@daytonaio/sdk` (v0.161.0) as a dependency of `packages/server`
- Created new file `packages/server/src/services/daytona-client.ts`
- Defined `DaytonaClient` Effect `Context.Tag` with identifier `@laborer/DaytonaClient`
- Service interface exposes 7 Effect-wrapped SDK operations:
  - `create(params, options?)` — create sandbox from image
  - `createFromSnapshot(params, options?)` — create sandbox from snapshot
  - `get(sandboxIdOrName)` — get sandbox by ID or name
  - `list(labels?, page?, limit?)` — list sandboxes with optional label filter
  - `start(sandbox, timeout?)` — start a stopped sandbox
  - `stop(sandbox)` — stop a running sandbox
  - `delete(sandbox, timeout?)` — permanently delete a sandbox
- Also exposes `snapshot` (Daytona SnapshotService) and `raw` (Daytona SDK instance) for direct access
- `DaytonaClient.layer` reads `DAYTONA_API_KEY`, `DAYTONA_API_URL`, `DAYTONA_TARGET` from env
  and constructs the `Daytona` SDK instance. Dies if `DAYTONA_API_KEY` is not set.
- Error mapping: SDK errors -> `RpcError` with distinct codes:
  - `DaytonaNotFoundError` -> `DAYTONA_NOT_FOUND`
  - `DaytonaRateLimitError` -> `DAYTONA_RATE_LIMIT`
  - `DaytonaTimeoutError` -> `DAYTONA_TIMEOUT`
  - Other errors -> `DAYTONA_ERROR` / `DAYTONA_UNKNOWN`
- Exported type aliases for consumers: `DaytonaSandbox`, `DaytonaPaginatedSandboxes`,
  `CreateSandboxFromImageParams`, `CreateSandboxFromSnapshotParams`

### Design decisions:
- `PaginatedSandboxes` is not exported from `@daytonaio/sdk` public API, so we use
  `Awaited<ReturnType<Daytona['list']>>` as the `DaytonaPaginatedSandboxes` type alias
- Layer uses `Effect.die` (defect, not typed error) for missing API key since this is
  a configuration error that should crash early, not be handled by callers
- Each SDK method wrapped with `Effect.fn` for call-site tracing
- `snapshot` sub-service exposed directly (not wrapped) since snapshot operations are
  varied and wrapping each adds minimal value
- `raw` Daytona instance exposed for cases needing direct SDK access (e.g., sandbox
  instance methods like `createSshAccess`, `getPreviewLink`)
- No test files added — the DaytonaClient layer requires a real API key to construct.
  Integration tests (Daytona-required) will be added as part of downstream issues.

### Verification:
- Server typecheck passes (`tsc --noEmit`)
- 410 server tests pass (48 test files)
- 89 shared tests pass
- `bun run format` passes (488 files, no fixes)

## Issue 13: DaytonaSandboxProvider — create sandbox

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Created new file `packages/server/src/services/daytona-sandbox-provider.ts`
- Defined `DaytonaSandboxProvider` as a `Context.Tag` that produces a `SandboxProvider` value
- `DaytonaSandboxProvider.layer` depends on `DaytonaClient` and `LaborerStore`
- All 8 `SandboxProvider` interface methods implemented:
  - `createSandbox`: Core Daytona integration — creates a cloud sandbox for a workspace
    - Determines image from `devServer.image` or uses Daytona default (via `createFromSnapshot`)
    - Creates sandbox via `DaytonaClient.create()` with TypeScript language, labeler labels
      (`laborer-workspace-id`, `laborer-project`, `laborer-branch`), auto-stop (15min),
      auto-archive (7 days), auto-delete disabled (-1)
    - Reports progress via `v2.SandboxSetupStepChanged` events (`creating-sandbox`, `starting-sandbox`)
    - Commits `v2.SandboxStarted` event with `sandboxProvider: "daytona"`
    - Invokes `onReady` callback after successful creation
  - `destroySandbox`: Looks up sandbox from LiveStore, calls `DaytonaClient.get()` then
    `DaytonaClient.delete()`, handles already-destroyed (404) gracefully, commits `v2.SandboxStopped`
  - `pauseSandbox`: Calls `DaytonaClient.stop()`, commits `v2.SandboxPaused`
  - `resumeSandbox`: Calls `DaytonaClient.start()`, commits `v2.SandboxResumed`
  - `getPreviewUrl`: Calls `sandbox.getPreviewLink(port)` via SDK, returns URL string
  - `spawnTerminal`: Stub returning `NOT_IMPLEMENTED` error (Issue 16)
  - `reconcileState`: Stub logging debug message (Issue 20)
  - `checkAvailability`: Calls `DaytonaClient.list(limit=1)` as connectivity check

### Design decisions:
- When `devServer.image` is null, uses `DaytonaClient.createFromSnapshot` (no snapshot param =
  Daytona default image which includes Node 22, bun, git, Claude Code, OpenCode, Codex)
- When `devServer.image` is set, uses `DaytonaClient.create` with `CreateSandboxFromImageParams`
- `sandboxUrl` column stores the sandbox ID (Daytona preview URLs are constructed per-port
  via `sandbox.getPreviewLink()`)
- `sandboxImage` stored as `'daytona-default'` when using the default image
- Destroy is best-effort: logs warnings on errors but doesn't fail the destroy
- Destroy is idempotent: handles already-destroyed sandboxes gracefully (catches 404)
- Pause/resume call Daytona stop/start respectively (Daytona terminology)

### Tests added (9 tests in packages/server/test/daytona-sandbox-provider.test.ts):
- createSandbox: calls `create` with correct SDK params when `devServer.image` is set
- createSandbox: calls `createFromSnapshot` when `devServer.image` is null (Daytona default)
- createSandbox: invokes `onReady` callback after creation
- createSandbox: sets labels with workspace ID, project name, and branch name
- destroySandbox: deletes sandbox and commits `SandboxStopped`
- destroySandbox: handles already-destroyed sandbox gracefully (NOT_FOUND)
- pauseSandbox: stops sandbox and commits `SandboxPaused`
- resumeSandbox: starts sandbox and commits `SandboxResumed`
- checkAvailability: returns available when `DaytonaClient.list` succeeds

### Test approach:
- Uses mutable `MockCallRecord[]` array shared via closure with `Layer.succeed`-based
  mock `DaytonaClient` — avoids Layer memoization issues with Effect `Ref`-based call logs
- `DaytonaSandboxProvider.layer` composed with mock `DaytonaClient` + `TestLaborerStore`
- Separate `makeNotFoundClientLayer` for destroy idempotency test (`.get()` returns `DAYTONA_NOT_FOUND`)
- Each test gets fresh `callLog` array and fresh Layer via factory functions

### Verification:
- Server typecheck passes (`tsc --noEmit`)
- 419 server tests pass (50 test files, 1 skipped)
- 89 shared tests pass
- `bun run format` passes (490 files, no fixes)

## Issue 3: RPC rename — container.* to sandbox.* with backward-compat aliases

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Added 5 new provider-agnostic RPC definitions in `packages/shared/src/rpc.ts`:
  - `sandbox.providerStatus` (replaces `docker.status`) — uses same `DockerStatusResponse` schema via `ProviderStatusResponse` alias
  - `workspace.startSandbox` (replaces `workspace.startContainer`) — same payload shape
  - `sandbox.setPort` (replaces `container.setPort`) — same payload shape
  - `sandbox.pause` (replaces `container.pause`) — same payload shape
  - `sandbox.resume` (replaces `container.unpause`) — same payload shape
- Old RPC names (`docker.status`, `workspace.startContainer`, `container.setPort`,
  `container.pause`, `container.unpause`) retained as backward-compatible aliases —
  both old and new names have active handlers
- Added handlers for all 5 new RPC names in `packages/server/src/rpc/handlers.ts`:
  - `sandbox.providerStatus` delegates to `SandboxProvider.checkAvailability()`
  - `workspace.startSandbox` delegates to `WorkspaceProvider.startSandbox()`
  - `sandbox.setPort` commits `v2.sandboxPortChanged` event (not `v1.containerPortChanged`)
  - `sandbox.pause` delegates to `SandboxProvider.pauseSandbox()`
  - `sandbox.resume` delegates to `SandboxProvider.resumeSandbox()` with same
    CONTAINER_NOT_FOUND fallback-to-recreate logic as old `container.unpause`
- Old handlers unchanged — they still call `ContainerService` / `DockerDetection` directly
- New handlers route through `SandboxProvider` abstraction (provider-agnostic)

### Infrastructure changes:
- Added `SandboxProvider` to `DeferredServicesProxyLive` in `utility-main.ts`:
  - Added `makeRefDelegatingService(SandboxProvider, { checkAvailability: ... })` proxy
  - Changed `DockerSandboxProvider.layer` from `Layer.provide` to `Layer.provideMerge` in
    `DeferredServiceStack` so `SandboxProvider` appears in stack output context
  - Added `Ref.set(sandboxProvider.ref, Context.get(stackCtx, SandboxProvider))` ref swap
  - Added `Context.add(SandboxProvider, sandboxProvider.proxy)` to return context
- Updated `test-layer.ts`: Changed `DockerSandboxProvider.layer` to `Layer.provideMerge`
  so `SandboxProvider` is in test stack output context
- Updated `core-layers.test.ts`: Added `SandboxProvider` stub to `DeferredServiceStubs`
  with `checkAvailability` placeholder override (returns `{ available: false }`)
- Updated `messageport-rpc.test.ts`: Added same `SandboxProvider` stub

### Design decisions:
- New sandbox.* handlers use `SandboxProvider` abstraction while old container.* handlers
  continue to use `ContainerService`/`DockerDetection` directly — this preserves exact
  backward compatibility for the old paths while new paths go through the abstraction
- `sandbox.setPort` uses `v2.sandboxPortChanged` event (already defined in Issue 2)
  while `container.setPort` continues to use `v1.containerPortChanged` — both events
  materialize to the same `sandboxPort` column
- `sandbox.providerStatus` has no error channel (matches `docker.status`) — the
  `checkAvailability` method returns `{ available: false }` on failure rather than failing

### Verification:
- All typechecks pass (shared, server, web — `tsc --noEmit`)
- 89 shared tests pass (5 test files)
- 419 server tests pass (50 test files, 1 skipped)
- 1070 web tests pass (73 test files, 1 skipped)
- `bun run format` passes (490 files, no fixes)

## Issue 4: UI — update all components to use sandbox* column names + RPC names

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Updated `workspace-list.tsx` — all RPC mutations switched to new names:
  - `workspace.startContainer` -> `workspace.startSandbox`
  - `container.setPort` -> `sandbox.setPort`
  - `container.pause` -> `sandbox.pause`
  - `container.unpause` -> `sandbox.resume`
- Renamed UI components to provider-agnostic names:
  - `ContainerPauseButton` -> `SandboxPauseButton`
  - `ContainerPortButton` -> `SandboxPortButton`
  - `StartContainerButton` -> `StartSandboxButton`
  - `getContainerSetupLabel` -> `getSandboxSetupLabel`
- Updated all user-facing strings from "container" to "sandbox" (aria-labels, toast messages,
  tooltip content, comments)
- Updated `getSandboxSetupLabel` to include Daytona-specific setup steps:
  `creating-sandbox`, `building-snapshot`, `pushing-code`, `configuring-ssh`, `starting-sandbox`
  alongside existing Docker steps (`building-image`, `starting-container`)
- Updated internal variable names: `isContainerized` -> `isSandboxed`,
  `isContainerPaused` -> `isSandboxPaused`, `hasContainerConfig` -> `hasSandboxConfig`,
  `containerLink` -> `sandboxLink`, `handleContainerLinkClick` -> `handleSandboxLinkClick`,
  `isStartingContainer` -> `isStartingSandbox`, `handleStartContainer` -> `handleStartSandbox`
- Replaced `docker-status-banner.tsx` with `provider-status-banner.tsx`:
  - `DockerStatusBanner` -> `ProviderStatusBanner`
  - `docker.status` RPC -> `sandbox.providerStatus` RPC
  - Updated banner title from "Docker not available" to "Sandbox provider not available"
  - Deleted old `docker-status-banner.tsx` file
- Updated `__root.tsx` to import `ProviderStatusBanner` from new file
- Updated test fixtures:
  - `workspace-card-layout.test.tsx`: `PAUSE_CONTAINER_RE` -> `PAUSE_SANDBOX_RE`,
    `RESUME_CONTAINER_RE` -> `RESUME_SANDBOX_RE`, updated describe/it descriptions
  - `workspace-plan-scope.test.tsx`: `START_CONTAINER_RE` -> `START_SANDBOX_RE`,
    updated comment

### Verification:
- All 7 turbo typecheck tasks pass (`tsc --noEmit`)
- 1070 web tests pass (73 test files, 1 skipped)
- 419 server tests pass (49 test files, 1 skipped)
- 89 shared tests pass
- `bun run format` passes (490 files, no fixes)
- No remaining `container*` column references in `apps/web/src/`
- No remaining old RPC names (`container.pause`, `docker.status`, etc.) in `apps/web/src/`

## Issue 5: Provider config — add provider field to laborer.json + config resolution

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Extended `DevServerConfig` interface in `packages/server/src/services/config-service.ts` with:
  - `provider?: "docker" | "daytona"` — per-project sandbox provider selection
  - `resources?: { cpu?: number; memory?: number; disk?: number }` — Daytona sandbox resource limits
  - `autoStopInterval?: number` — minutes of inactivity before auto-stop (Daytona only)
- Added `SandboxProviderType` type alias and `VALID_SANDBOX_PROVIDERS` constant
- Added `SandboxResources` interface for resource limits
- Extended `ResolvedDevServerConfig` with resolved versions of all three new fields:
  - `provider: ResolvedValue<SandboxProviderType | null>` (default: null)
  - `resources: ResolvedValue<SandboxResources | null>` (default: null)
  - `autoStopInterval: ResolvedValue<number | null>` (default: null)
- Updated `mergeDevServerConfig` to resolve new fields across config layers (closest wins)
- Updated `mergeDevServerUpdates` to merge new fields (also added missing `port` merge)
- Updated `validateDevServerConfig` to reject invalid `provider` values
- Extended `DevServerConfigResponse` RPC schema in `packages/shared/src/rpc.ts` with:
  - `autoStopInterval`, `provider`, `resources` fields
  - `SandboxProviderTypeSchema`, `SandboxResourcesSchema` exported schemas
- Extended `config.update` RPC payload to accept new devServer fields
- Refactored `handleConfigUpdate` in `packages/server/src/rpc/handlers.ts`:
  - Extracted `validateDevServerUpdate` helper function to reduce complexity
  - Added validation for `provider`, `autoStopInterval`, `resources`
  - Added helper validators: `isOptionalString`, `isOptionalBoolean`,
    `isOptionalPositiveNumber`, `isOptionalNumber`, `isOptionalStringArray`,
    `isValidSandboxResources`
- Updated `CreateSandboxParams.devServerConfig` in `sandbox-provider.ts` with new fields
- Updated `performSandboxSetup` in `workspace-provider.ts` to pass through new fields
- Updated test fixtures:
  - `test/daytona-sandbox-provider.test.ts`: `dsc()` helper includes new fields
  - `test/rpc/config-management.test.ts`: expected config objects include new fields

### Tests added (10 new tests in packages/server/test/config-service.test.ts):
- Returns null defaults for provider, resources, autoStopInterval
- Reads devServer.provider "daytona" from project config
- Reads devServer.provider "docker" from project config
- Rejects invalid provider value
- Reads devServer.autoStopInterval from project config
- Reads devServer.resources from project config
- Inherits provider from ancestor config
- Overrides ancestor provider with project config
- Writes and round-trips provider config (provider + autoStopInterval + resources)

### Design decisions:
- `provider` defaults to `null` (not `"docker"`) — resolution against the global default
  is deferred to Issue 6. A null provider means "use whatever the global default is"
- `resources` is typed as `SandboxResources | null` (nullable object) rather than
  individual nullable fields, matching how Daytona SDK accepts resource params
- `autoStopInterval` is a plain number (minutes), not a Duration, matching Daytona's API
- Validation rejects invalid provider values at the `validateDevServerConfig` level
  (same as the existing image/dockerfile mutual exclusion validation)
- New types `SandboxProviderType` and `SandboxResources` are exported for use by other
  packages (e.g., the Daytona SDK client)

### Verification:
- All 15 turbo tasks pass (typecheck + format + test across all packages)
- 428 server tests pass (49 test files, 1 skipped)
- 89 shared tests pass
- 1070+ web tests pass
- `bun run format` passes (490 files, no fixes)

## Issue 14: DaytonaSandboxProvider — destroy sandbox

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Enhanced `destroySandbox` in `packages/server/src/services/daytona-sandbox-provider.ts`:
  - Improved error handling in `.get()` to distinguish DAYTONA_NOT_FOUND (sandbox already
    gone, skip delete) from other errors (transient failures — log warning, skip delete)
  - Added detailed step-by-step comments for the destroy flow
  - Added SSH config cleanup hook placeholder (TODO for Issue 22)
  - Added success log after successful SDK delete
  - Commits v2.SandboxStopped regardless of deletion outcome (best-effort semantics)
- Updated file header comment to reflect Issue 14 completion — no longer describes
  destroy as a stub
- Added error code to warning log messages for better debuggability

### Tests added (4 new tests in packages/server/test/daytona-sandbox-provider.test.ts):
- destroySandbox: skips gracefully when workspace not found in LiveStore (no SDK calls)
- destroySandbox: skips gracefully when workspace has no sandboxId (no SDK calls)
- destroySandbox: still commits SandboxStopped when get fails with non-NOT_FOUND error
  (DAYTONA_TIMEOUT — verifies delete is skipped but event is committed)
- destroySandbox: still commits SandboxStopped when delete fails (best-effort — verifies
  both get and delete are called, event is committed despite delete failure)

### Mock infrastructure added:
- `makeGetErrorClientLayer`: DaytonaClient mock where `.get()` returns DAYTONA_TIMEOUT
- `makeDeleteFailClientLayer`: DaytonaClient mock where `.get()` succeeds but `.delete()` fails
- Corresponding `makeGetErrorLayer` and `makeDeleteFailLayer` factory functions

### Verification:
- Server typecheck passes (`tsc --noEmit`)
- 432 server tests pass (49 test files, 1 skipped) — 13 total for daytona-sandbox-provider
- 89 shared tests pass
- `bun run format` passes (490 files, no fixes)

## Issue 19: Daytona sandbox pause/resume (stop/start) + auto-stop config

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Enhanced `pauseSandbox` in `packages/server/src/services/daytona-sandbox-provider.ts`:
  - Added idempotency: checks sandbox state via `DaytonaClient.get()` before calling `stop()`
  - If sandbox is already `stopped` or `archived`, skips the SDK stop call (logs debug message)
  - Still commits `v2.SandboxPaused` event regardless (syncs LiveStore to match actual state)
  - Added SSH config cleanup TODO hook for Issue 22
  - Improved logging with sandbox ID
- Enhanced `resumeSandbox` in `packages/server/src/services/daytona-sandbox-provider.ts`:
  - Added idempotency: checks sandbox state via `DaytonaClient.get()` before calling `start()`
  - If sandbox is already `started`, skips the SDK start call (logs debug message)
  - Still commits `v2.SandboxResumed` event regardless (syncs LiveStore to match actual state)
  - Added SSH config setup TODO hook for Issue 22
  - Improved logging with sandbox ID
- Added `setAutoStopInterval` to `SandboxProvider` interface (`packages/server/src/services/sandbox-provider.ts`):
  - New method: `setAutoStopInterval(workspaceId, interval) => Effect<void, RpcError>`
  - For Daytona: calls `sandbox.setAutostopInterval()` via the SDK
  - For Docker: no-op (containers don't have auto-stop)
- Implemented `setAutoStopInterval` in `DaytonaSandboxProvider`:
  - Looks up workspace → gets sandboxId
  - Fetches sandbox from Daytona API
  - Calls `DaytonaClient.setAutostopInterval(sandbox, interval)` to update the SDK
- Added no-op `setAutoStopInterval` in `DockerSandboxProvider`
- Added `setAutostopInterval` to `DaytonaClient` service (`packages/server/src/services/daytona-client.ts`):
  - New Effect-wrapped method: calls `sandbox.setAutostopInterval(interval)` via SDK
  - Error mapping to `RpcError` consistent with other SDK methods
- Added `sandbox.setAutoStop` RPC definition in `packages/shared/src/rpc.ts`:
  - Payload: `workspaceId: string`, `interval: Int` (minutes, 0 disables)
  - Error type: `RpcError`
- Added `sandbox.setAutoStop` handler in `packages/server/src/rpc/handlers.ts`:
  - Delegates to `SandboxProvider.setAutoStopInterval()`
- Updated file header comment in `daytona-sandbox-provider.ts` to document pause/resume flows
- Updated all mock `DaytonaClient` layers in tests to include `setAutostopInterval` method

### Tests added (8 new tests in packages/server/test/daytona-sandbox-provider.test.ts):
- pauseSandbox: skips stop call when sandbox is already stopped (idempotent)
- pauseSandbox: skips stop call when sandbox is archived (idempotent)
- pauseSandbox: returns NOT_FOUND when workspace has no sandbox
- resumeSandbox: skips start call when sandbox is already started (idempotent)
- resumeSandbox: returns NOT_FOUND when workspace has no sandbox
- setAutoStopInterval: calls setAutostopInterval on the SDK sandbox with correct interval
- setAutoStopInterval: calls setAutostopInterval with 0 to disable auto-stop
- setAutoStopInterval: returns NOT_FOUND when workspace has no sandbox

### Mock infrastructure added:
- `makeStateClientLayer`: DaytonaClient mock where `.get()` returns a sandbox with a
  configurable state (used for idempotency tests)
- `makeStateLayer`: Composed layer using `makeStateClientLayer` for specific sandbox states

### Existing test updated:
- `resumeSandbox > starts sandbox and commits SandboxResumed` now uses `makeStateLayer(log, 'stopped')`
  instead of `makeLayer(log)` since the default mock sandbox has `state: 'started'` and the
  idempotent implementation correctly skips the start call for already-started sandboxes

### Verification:
- All 7 turbo typecheck tasks pass (`tsc --noEmit`)
- 21 daytona-sandbox-provider tests pass (8 new + 13 existing)
- 73 total tests pass across key test files (daytona, config-service, core-layers)
- 58 RPC tests pass (12 test files)
- 89 shared tests pass
- `bun run format` passes (490 files, no fixes)

## Issue 15: Git sync — push worktree HEAD to Daytona sandbox via SSH

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Created new file `packages/server/src/lib/git-sync.ts` with pure helper functions:
  - `buildRemoteName(workspaceId)` — builds `sandbox-{workspaceId}` remote name
  - `buildSshRemoteUrl(token, host?, projectDir?)` — builds `ssh://{token}@{host}{projectDir}` URL
  - `buildAddRemoteArgs(remoteName, remoteUrl)` — git args for `remote add`
  - `buildPushArgs(remoteName)` — git args for `push HEAD:main --force`
  - `buildRemoveRemoteArgs(remoteName)` — git args for `remote remove`
  - `buildSandboxInitCommand(projectDir?)` — sandbox command for `git init` + `receive.denyCurrentBranch ignore`
  - `buildSandboxCheckoutCommand(projectDir?)` — sandbox command for `git checkout -f main`
  - `buildSshGitEnv()` — SSH env vars with `StrictHostKeyChecking=no`, `UserKnownHostsFile=/dev/null`, port 2222
  - Constants: `DAYTONA_SSH_HOST`, `DAYTONA_PROJECT_DIR`
- Added `pushCodeToSandbox` Effect function in `packages/server/src/services/daytona-sandbox-provider.ts`:
  - Step 1: Report progress `'pushing-code'` via `v2.SandboxSetupStepChanged`
  - Step 2: Get SSH access token from Daytona API (10-minute expiry) via `sandbox.createSshAccess(10)`
  - Step 3: Initialize git repo in sandbox via `sandbox.process.executeCommand(buildSandboxInitCommand())`
  - Step 4: Add temporary local git remote via `spawnGit(buildAddRemoteArgs(...))`
    - Handles remote already existing from a previous failed attempt (remove + retry)
  - Step 5: Push HEAD to sandbox via `spawnGit(buildPushArgs(...))` with SSH env, 2-minute timeout
  - Step 6: Checkout pushed code in sandbox via `sandbox.process.executeCommand(buildSandboxCheckoutCommand())`
  - Step 7: Clean up temporary git remote via `Effect.ensuring(...)` — always runs even on push failure
- Integrated `pushCodeToSandbox` into `createSandbox` flow:
  - Called after `v2.SandboxStarted` event and before `onReady` callback
  - Clears `sandboxSetupStep` to null after successful push (setup complete)
- Updated `makeMockSandbox` in tests to accept optional `log` parameter:
  - Added `createSshAccess` mock method returning test token `test-ssh-token-abc`
  - Added `process.executeCommand` mock method recording calls to log
- Added `vi.mock` for `spawnGit` in DaytonaSandboxProvider tests to prevent real SSH connections

### Tests added:

#### Pure function tests (16 tests in `packages/server/test/git-sync.test.ts`):
- `buildRemoteName` — sandbox-{workspaceId} format, UUID handling
- `buildSshRemoteUrl` — default host/dir, custom host, custom dir, token in URL
- `buildAddRemoteArgs` — correct git args array
- `buildPushArgs` — push with --force flag
- `buildRemoveRemoteArgs` — correct remove args
- `buildSandboxInitCommand` — git init + receive.denyCurrentBranch, custom dir
- `buildSandboxCheckoutCommand` — checkout -f main, custom dir
- `buildSshGitEnv` — StrictHostKeyChecking, UserKnownHostsFile, port 2222

#### Integration tests (5 new tests in `packages/server/test/daytona-sandbox-provider.test.ts`):
- createSandbox calls `createSshAccess` with 10-minute expiry during git sync
- createSandbox executes `git init` in sandbox before pushing code
- createSandbox executes `git checkout -f main` in sandbox after push
- createSandbox reports `pushing-code` setup step, then clears it (null) on completion
- createSandbox calls `git remote remove` to clean up after push

### Design decisions:
- SSH remote URL format: `ssh://{token}@ssh.app.daytona.io/home/daytona/project`
  matches the PRD specification for Daytona SSH gateway access
- `receive.denyCurrentBranch ignore` configured in sandbox git repo to allow
  pushes to a checked-out branch (sandbox starts with a working tree)
- `git push --force` used because the sandbox may have a different history
  (or none at all) on creation
- `Effect.ensuring` guarantees remote cleanup even on push failure,
  preventing orphaned git remotes in the local worktree
- Remote name collision handling: if `git remote add` fails (exit code != 0),
  attempts to remove the existing remote and retry once
- `spawnGit` with 120-second timeout for push (accommodates large repos)
- `vi.mock` for `spawnGit` in tests prevents real SSH connections while
  still testing the full Effect pipeline including SDK calls

### Verification:
- Server typecheck passes (`tsc --noEmit`)
- 461 server tests pass (50 test files, 1 skipped) — 26 daytona-sandbox-provider + 16 git-sync
- 89 shared tests pass
- `bun run format` passes (492 files, no fixes)

## Issue 16: Daytona PTY — WebSocket session creation + sendInput/resize

Status: COMPLETE
Date: 2026-04-07

### Changes made:
- Implemented `spawnTerminal` in `packages/server/src/services/daytona-sandbox-provider.ts`:
  - Replaces the `NOT_IMPLEMENTED` stub with full PTY session creation
  - Looks up workspace in LiveStore to get `sandboxId`
  - Fetches sandbox from Daytona API via `DaytonaClient.get(sandboxId)`
  - Creates a PTY session via `sandbox.process.createPty()` with:
    - Unique session ID (`crypto.randomUUID()`)
    - Terminal dimensions from opts (cols/rows, defaults 80×24)
    - Working directory: `/home/daytona/project`
    - Environment: `TERM=xterm-256color`, `COLORTERM=truecolor`
    - `onData` callback for terminal output (bridge point for Issue 17)
  - Waits for WebSocket connection via `ptyHandle.waitForConnection()`
  - Sends initial command as input if `opts.command` is specified
  - Stores the `PtyHandle` in an in-memory `Map` keyed by session ID
  - Returns a `TerminalHandle` with metadata (id, workspaceId, command, status)
- Added in-memory PTY handle registry (`daytonaPtyHandles` Map):
  - `getDaytonaPtyHandle(sessionId)` — retrieves a live PtyHandle for the
    xterm.js bridge (Issue 17) to wire I/O
  - `removeDaytonaPtyHandle(sessionId)` — cleans up handles when sessions end
  - Both functions exported for use by downstream Issue 17
- Updated file header comment to document the spawnTerminal flow
- Added `PtyHandle` type import from `@daytonaio/sdk`
- Added `DAYTONA_PROJECT_DIR` constant for sandbox working directory

### Tests added (7 new tests in packages/server/test/daytona-sandbox-provider.test.ts):
- spawnTerminal: creates PTY session and returns TerminalHandle with correct metadata
- spawnTerminal: calls createPty with correct parameters (cols, rows, cwd, envs, id)
- spawnTerminal: uses default cols/rows (80×24) when not specified
- spawnTerminal: waits for WebSocket connection to be established
- spawnTerminal: sends initial command as input when specified (with trailing newline)
- spawnTerminal: does not send input when no command specified
- spawnTerminal: returns NOT_FOUND when workspace has no sandbox

### Mock infrastructure added:
- `makeMockPtyHandle(sessionId, log)` — creates a mock PtyHandle with `sendInput`,
  `resize`, `waitForConnection`, `disconnect`, `kill`, and `wait` methods that
  record calls to the shared log
- Updated `makeMockSandbox` to include `process.createPty` mock that creates
  mock PtyHandles and records calls

### Design decisions:
- PTY handles stored in a module-level `Map<string, PtyHandle>` rather than
  in LiveStore or Effect Ref — PTY sessions are transient WebSocket connections,
  not persistent state. The Map is the appropriate lifetime scope.
- `onData` callback in `createPty` is a no-op placeholder — the actual xterm.js
  bridge (Issue 17) will read output from the PtyHandle directly. The callback
  is required by the SDK's `PtyConnectOptions` type.
- `waitForConnection()` is called before returning the handle to ensure the
  WebSocket is fully established and ready for input. This prevents race
  conditions where sendInput is called before the PTY is connected.
- When `opts.command` is specified, a trailing `\n` is appended to simulate
  pressing Enter, matching the Docker terminal flow behavior.
- Default shell is `/bin/sh` when no command is specified (consistent with
  Docker terminal defaults).
- `getDaytonaPtyHandle` and `removeDaytonaPtyHandle` are exported as named
  functions (not as part of the service interface) since they are used by the
  terminal bridge layer, not by `WorkspaceProvider` or RPC handlers.

### Verification:
- Server typecheck passes (`tsc --noEmit`)
- 468 server tests pass (50 test files, 1 skipped) — 33 daytona-sandbox-provider + 16 git-sync
- `bun run format` passes (492 files, no fixes)
