2026-03-06
- Selected Issue 175 as the highest-priority next task because it is foundational PRD data-model work and unblocks both storage and UI follow-up issues.
- Added `PrdId` and `PrdStatus` to `packages/shared/src/types.ts` and introduced the `Prd` domain model.
- Added the `prds` LiveStore table plus `prdCreated`, `prdStatusChanged`, and `prdRemoved` synced events/materializers in `packages/shared/src/schema.ts`, and registered `prds` in both schema exports and `activeTables`.
- Added `packages/server/test/prd-schema.test.ts` to verify PRD lifecycle materialization, supported status values, and that existing task materialization still works.
- Verified with `bun run check-types` in `packages/shared` and `bun run test -- prd-schema.test.ts` in `packages/server`.
- Selected Issue 176 as the highest-priority next task because PRD-linked tasks are the critical dependency for `prd.createIssue`, remaining-issues queries, and plan progress tracking.
- Added nullable `prdId` support across the shared task model in `packages/shared/src/schema.ts`, `packages/shared/src/types.ts`, and `packages/shared/src/rpc.ts` so PRD tasks can be linked without breaking existing task records.
- Updated `packages/server/src/services/task-manager.ts` and `packages/server/src/rpc/handlers.ts` so task creation can carry an optional `prdId` and preserve it in RPC responses.
- Extended `packages/server/test/prd-schema.test.ts` and added `packages/server/test/task-manager.test.ts`, while updating existing importer tests to cover the new task payload shape.
- Verified with `bun run check-types` in `packages/shared`, `bun run check-types` in `packages/server`, `bun run test -- prd-schema.test.ts task-manager.test.ts github-task-importer.test.ts linear-task-importer.test.ts prd-task-importer.test.ts` in `packages/server`, and `bun x ultracite check` on all touched files.
- Selected Issue 177 as the highest-priority next task because PRD file storage is the core server capability needed before any PRD RPCs or MCP tooling can persist plans.
- Added `PrdStorageService` in `packages/server/src/services/prd-storage-service.ts` with Effect service wiring, PRD slug/file-name helpers, automatic PRD directory creation, and atomic temp-file writes for markdown files.
- Extended `packages/server/src/services/config-service.ts`, `packages/server/src/rpc/handlers.ts`, and `packages/shared/src/rpc.ts` to support a layered `prdsDir` config value alongside existing `laborer.json` settings.
- Expanded `packages/server/test/config-service.test.ts` and added `packages/server/test/prd-storage-service.test.ts` to cover `prdsDir` resolution, default `<worktreeDir>/prds` behavior, slug generation, PRD creation, and PRD reads.
- Verified with `bun run check-types` in `packages/shared`, `bun run check-types` in `packages/server`, `bun run test -- config-service.test.ts prd-storage-service.test.ts` in `packages/server`, and `bun x ultracite check` on all touched files.
- Selected Issue 178 as the highest-priority next task because it is the first server RPC slice that turns PRD storage into usable product functionality and unblocks both downstream PRD CRUD and issue-creation work.
- Added `PrdResponse` plus `prd.create` and `prd.list` RPC definitions in `packages/shared/src/rpc.ts` so PRD metadata is part of the shared server contract.
- Implemented `handlePrdCreate` and `handlePrdList` in `packages/server/src/rpc/handlers.ts`, including title validation, duplicate detection per project, PRD file writes through `PrdStorageService`, and `prdCreated` event commits to LiveStore.
- Wired `PrdStorageService.layer` into `packages/server/src/main.ts` and added `packages/server/test/prd-rpc-handlers.test.ts` to cover create/list behavior and duplicate-title rejection.
- Verified with `bun run check-types` in `packages/shared`, `bun run check-types` in `packages/server`, `bun run test -- prd-rpc-handlers.test.ts` in `packages/server`, and `bun x ultracite check packages/shared/src/rpc.ts packages/server/src/rpc/handlers.ts packages/server/src/main.ts packages/server/test/prd-rpc-handlers.test.ts`.
- Selected Issue 184 as the highest-priority next task because the MCP package scaffold is the foundational integration point for both agent-facing PRD tools and downstream auto-registration work, while also being independent of the still-blocked PRD update chain.
- Added `packages/mcp/` with Bun/Effect package wiring, stdio `McpServer` startup in `packages/mcp/src/main.ts`, a shared Laborer RPC client service, and a `ProjectDiscovery` service that resolves the current project by walking up from cwd against registered project repo paths.
- Extended the shared/server RPC contract with `project.list` in `packages/shared/src/rpc.ts` and `packages/server/src/rpc/handlers.ts` so the MCP package can discover registered projects through the main server instead of reading store state directly.
- Added `packages/mcp/test/project-discovery.test.ts` to cover direct matches, parent-directory discovery, RPC-backed lookup, and the not-found error path, plus `packages/server/test/project-rpc-handlers.test.ts` for the new `project.list` handler.
- Verified with `bun install` at the repo root, `bun run build` and `bun run test` in `packages/mcp`, `bun run check-types` in `packages/shared` and `packages/server`, `bun run test -- project-rpc-handlers.test.ts` in `packages/server`, and `bun x ultracite check` on the touched MCP/server/shared files.
- Selected Issue 181 as the highest-priority ready task because PRD issue creation is the first end-to-end bridge from saved plans into executable task records, and it unblocks remaining-issues queries plus the UI removal of manual task creation.
- Added `prd.createIssue` to `packages/shared/src/rpc.ts` and implemented `handlePrdCreateIssue` in `packages/server/src/rpc/handlers.ts`, including PRD lookup, input validation, companion issues-file appends, PRD task creation, and PRD-aware task responses.
- Extended `packages/server/src/services/prd-storage-service.ts` with companion issues-file path derivation and append logic that creates `PRD-<slug>-issues.md`, numbers `## Issue <n>` sections, and preserves separator formatting between issues.
- Expanded `packages/server/test/prd-rpc-handlers.test.ts` and `packages/server/test/prd-storage-service.test.ts` to cover issue creation success, missing-PRD failures, issue numbering, and issues-file creation/appends.
- Verified with `bun run check-types` in `packages/shared`, `bun run check-types` in `packages/server`, `bun run test -- prd-rpc-handlers.test.ts prd-storage-service.test.ts` in `packages/server`, and `bun x ultracite check packages/shared/src/rpc.ts packages/server/src/rpc/handlers.ts packages/server/src/services/prd-storage-service.ts packages/server/test/prd-rpc-handlers.test.ts packages/server/test/prd-storage-service.test.ts`.
- Selected Issue 179 as the highest-priority ready task because it closes the remaining core PRD CRUD gap on the server and directly unblocks PRD updates plus the MCP read/remove tools.
- Added `prd.read` and `prd.remove` to `packages/shared/src/rpc.ts`, including a PRD read response that returns persisted metadata alongside markdown content.
- Implemented `handlePrdRead` and `handlePrdRemove` in `packages/server/src/rpc/handlers.ts`, covering PRD lookup, file reads, linked PRD-task cleanup, and `prdRemoved` event commits.
- Extended `packages/server/src/services/prd-storage-service.ts` with `removePrdArtifacts` so PRD markdown files and their companion issues files are deleted together.
- Expanded `packages/server/test/prd-rpc-handlers.test.ts` and `packages/server/test/prd-storage-service.test.ts` to cover PRD read/remove success paths, missing-PRD errors, linked-task cleanup, and file deletion behavior.
- Verified with `bun run check-types` in `packages/shared`, `bun run check-types` in `packages/server`, `bun run test -- prd-rpc-handlers.test.ts prd-storage-service.test.ts` in `packages/server`, and `bun x ultracite check packages/shared/src/rpc.ts packages/server/src/rpc/handlers.ts packages/server/src/services/prd-storage-service.ts packages/server/test/prd-rpc-handlers.test.ts packages/server/test/prd-storage-service.test.ts`.
- Selected Issue 180 as the highest-priority ready task because PRD updates are the last missing core server mutation for plan editing, and completing them unblocks the MCP PRD toolset without branching into UI work.
- Added `prd.update` and `prd.updateStatus` to `packages/shared/src/rpc.ts`, plus a new `prdUpdated` synced event/materializer in `packages/shared/src/schema.ts` so PRD edits and status changes are part of the shared contract and store lifecycle.
- Implemented `handlePrdUpdate` and `handlePrdUpdateStatus` in `packages/server/src/rpc/handlers.ts`, including missing-PRD checks, PRD status validation, file overwrites, `prdUpdated` commits, and `prdStatusChanged` commits.
- Extended `packages/server/src/services/prd-storage-service.ts` with `updatePrdFile` for atomic PRD overwrites, and expanded `packages/server/test/prd-rpc-handlers.test.ts`, `packages/server/test/prd-storage-service.test.ts`, and `packages/server/test/prd-schema.test.ts` to cover update flows, invalid status rejection, and PRD update materialization.
- Verified with `bun run check-types` in `packages/shared`, `bun run check-types` in `packages/server`, `bun run test -- prd-rpc-handlers.test.ts prd-storage-service.test.ts prd-schema.test.ts` in `packages/server`, and `bun x ultracite check packages/shared/src/schema.ts packages/shared/src/rpc.ts packages/server/src/services/prd-storage-service.ts packages/server/src/rpc/handlers.ts packages/server/test/prd-rpc-handlers.test.ts packages/server/test/prd-storage-service.test.ts packages/server/test/prd-schema.test.ts`.
- Selected Issue 182 as the highest-priority ready task because it completes the core server-side issue query surface, unblocks `prd.updateIssue`, and gives both MCP and UI flows a way to read issue markdown plus fetch only actionable PRD tasks.
- Added `prd.readIssues` and `prd.listRemainingIssues` to `packages/shared/src/rpc.ts` so the shared RPC contract now exposes PRD issue-file reads and filtered remaining-issue queries.
- Implemented `handlePrdReadIssues` and `handlePrdListRemainingIssues` in `packages/server/src/rpc/handlers.ts`, including missing-PRD validation, source/prdId/status filtering, and handler wiring in `LaborerRpcsLive`.
- Extended `packages/server/src/services/prd-storage-service.ts` with `readIssuesFile`, returning the companion issues markdown when present and an empty string when the issues file has not been created yet.
- Expanded `packages/server/test/prd-rpc-handlers.test.ts` and `packages/server/test/prd-storage-service.test.ts` to cover issues-file reads, empty-file behavior, remaining-issue filtering, and not-found error cases.
- Verified with `bun run check-types` in `packages/shared`, `bun run check-types` in `packages/server`, `bun run test -- prd-rpc-handlers.test.ts prd-storage-service.test.ts` in `packages/server`, and `bun x ultracite check packages/shared/src/rpc.ts packages/server/src/services/prd-storage-service.ts packages/server/src/rpc/handlers.ts packages/server/test/prd-rpc-handlers.test.ts packages/server/test/prd-storage-service.test.ts`.
- Selected Issue 183 as the highest-priority ready task because it finishes the core server-side PRD issue mutation flow and directly unblocks the MCP issue toolset in Issue 186.
- Added `prd.updateIssue` to `packages/shared/src/rpc.ts` and implemented `handlePrdUpdateIssue` in `packages/server/src/rpc/handlers.ts`, including task lookup, PRD validation, optional markdown-body replacement, optional status updates through `TaskManager`, and PRD-aware response shaping.
- Extended `packages/server/src/services/prd-storage-service.ts` with `updateIssue`, plus issue-section parsing helpers that target the matching issue by numbered external ID fallback and rewrite only that markdown section while leaving neighboring issues intact.
- Expanded `packages/server/test/prd-rpc-handlers.test.ts` and `packages/server/test/prd-storage-service.test.ts` to cover body-plus-status updates, status-only updates, missing-task failures, and single-section issue-file preservation.
- Verified with `bun run check-types` in `packages/shared`, `bun run check-types` in `packages/server`, `bun run test -- prd-rpc-handlers.test.ts prd-storage-service.test.ts` in `packages/server`, and `bun x ultracite check packages/shared/src/rpc.ts packages/server/src/rpc/handlers.ts packages/server/src/services/prd-storage-service.ts packages/server/test/prd-rpc-handlers.test.ts packages/server/test/prd-storage-service.test.ts`.
- Selected Issue 185 as the highest-priority ready task because it is the thinnest remaining slice that exposes the finished PRD server RPCs to AI agents and directly unblocks the legacy PRD-flow removal in Issue 195.
- Added `packages/mcp/src/tools/prd-tools.ts` with Effect AI toolkit registrations for `create_prd`, `read_prd`, `update_prd`, and `list_prds`, including current-project discovery, exact-title PRD lookup for reads, typed Effect Schema inputs, and RPC error propagation.
- Extended `packages/mcp/src/services/laborer-rpc-client.ts` with typed PRD RPC helpers and wired `PrdToolsLayer` into `packages/mcp/src/main.ts` so the stdio MCP server now registers the PRD-management tools at startup.
- Added `packages/mcp/test/prd-tools.test.ts` and updated `packages/mcp/test/project-discovery.test.ts` to cover tool registration, discovered-project delegation, title-based PRD reads, invalid-input errors, and the expanded RPC client test shape.
- Verified with `bun run check-types`, `bun run build`, `bun run test -- prd-tools.test.ts project-discovery.test.ts`, and `bun x ultracite check src/main.ts src/services/laborer-rpc-client.ts src/tools/prd-tools.ts test/prd-tools.test.ts test/project-discovery.test.ts` in `packages/mcp`.
- Selected Issue 186 as the highest-priority ready task because it completes the MCP issue-management surface, directly unblocks plan-scoped rlph work in Issue 193, and is a smaller integration slice than the UI and registrar features.
- Added `packages/mcp/src/tools/issue-tools.ts` with Effect AI toolkit registrations for `create_issue`, `read_issues`, `update_issue`, and `list_remaining_issues`, including typed input schemas, current-project validation, and direct delegation to the finished `prd.*` RPCs.
- Extended `packages/mcp/src/services/laborer-rpc-client.ts` with typed issue RPC helpers and merged `IssueToolsLayer` into `packages/mcp/src/main.ts` so the stdio MCP server now exposes both PRD and issue tools at startup.
- Added `packages/mcp/test/issue-tools.test.ts` and updated `packages/mcp/test/prd-tools.test.ts` plus `packages/mcp/test/project-discovery.test.ts` to cover tool registration, issue-tool RPC delegation, and the expanded client stub surface.
- Verified with `bun run check-types`, `bun run build`, `bun run test -- issue-tools.test.ts prd-tools.test.ts project-discovery.test.ts`, and `bun x ultracite check src/main.ts src/services/laborer-rpc-client.ts src/tools/issue-tools.ts test/issue-tools.test.ts test/prd-tools.test.ts test/project-discovery.test.ts` in `packages/mcp`.
- Selected Issue 189 as the highest-priority ready task because it is the first UI slice in the plan workflow and directly unblocks the editor, issues list, and plan-workspace follow-up issues.
- Added `apps/web/src/components/plan-list.tsx` and wired it into `apps/web/src/components/project-group.tsx`, introducing a collapsible Plans subsection that queries LiveStore PRDs and PRD tasks per project, orders plans newest-first, shows completed/total issue progress, and preserves local selected styling for clicked plan rows.
- Added `apps/web/test/plan-list.test.tsx` to cover newest-first ordering, progress calculations scoped to PRD-backed tasks, empty-state rendering, and collapsible behavior.
- Verified with `bun run check-types`, `bun run test -- plan-list.test.tsx`, and `bun x ultracite check src/components/plan-list.tsx src/components/project-group.tsx test/plan-list.test.tsx` in `apps/web`.
- Selected Issue 187 as the highest-priority ready task because MCP auto-registration is a core part of the PRD workflow, it keeps AI-tool setup automatic for users, and it directly unblocks the remaining multi-client registrar work in Issue 188.
- Added `packages/server/src/services/mcp-registrar.ts` with an Effect-tagged `McpRegistrar` service, idempotent Opencode config merging, atomic JSON writes, startup registration, and warning-only failure handling that preserves unrelated `mcpServers` entries.
- Wired `packages/server/src/main.ts` to provide `McpRegistrar.layer` during server startup so the Opencode MCP entry is registered automatically once the server store layer is available.
- Added `packages/server/test/mcp-registrar.test.ts` to cover config merging, missing-file creation, preservation of existing MCP servers, idempotent no-rewrite behavior, and startup-time registration.
- Verified with `bun run check-types`, `bun run test -- mcp-registrar.test.ts`, and `bun x ultracite check src/main.ts src/services/mcp-registrar.ts test/mcp-registrar.test.ts` in `packages/server`.
- Selected Issue 188 as the highest-priority ready task because completing multi-client MCP auto-registration closes the remaining core agent-setup gap before the larger plan-editor UI slice, and it finishes the registrar work already started in Issue 187.
- Extended `packages/server/src/services/mcp-registrar.ts` so startup registration now covers Claude Code and Codex in addition to Opencode, using `~/.claude.json` JSON merging for Claude and `~/.codex/config.toml` section management for Codex while preserving unrelated user config.
- Expanded `packages/server/test/mcp-registrar.test.ts` to cover Claude config creation, Codex TOML block creation and replacement, idempotent no-rewrite behavior, and end-to-end startup registration across all three targets.
- Verified with `bun run check-types`, `bun run test -- mcp-registrar.test.ts`, and `bun x ultracite check src/services/mcp-registrar.ts test/mcp-registrar.test.ts` in `packages/server`.
- Selected Issue 195 as the highest-priority ready task because the new MCP PRD tooling is already in place, and removing the legacy rlph PRD-writing path makes the product workflow consistent before investing in the larger plan-detail UI work.
- Deleted the legacy `apps/web/src/components/write-prd-form.tsx` UI entry point and removed its workspace-card integration from `apps/web/src/components/workspace-list.tsx`, leaving the existing rlph loop/review/fix actions intact.
- Removed the deprecated `rlph.writePRD` RPC from `packages/shared/src/rpc.ts` and `packages/server/src/rpc/handlers.ts`, and deleted the obsolete `packages/server/src/services/prd-task-importer.ts` service plus its dedicated test file.
- Updated `packages/server/src/main.ts` to stop providing `PrdTaskImporter.layer`, fully removing the old terminal-output scraping flow from server startup.
- Verified with `bun run check-types` in `packages/shared`, `packages/server`, `packages/mcp`, and `apps/web`; `bun run test` in `packages/server`, `packages/mcp`, and `apps/web`; and `bun x ultracite check apps/web/src/components/workspace-list.tsx packages/shared/src/rpc.ts packages/server/src/rpc/handlers.ts packages/server/src/main.ts`.
- Selected Issue 190 as the highest-priority ready task because it is on the critical path: it unblocks Issue 191 (issues list), which unblocks 192 (workspace creation), which unblocks 193 (scoped task list), which unblocks 196 (final polish). Issue 194 (removal) is a simpler leaf task that only gates the final polish issue.
- Installed Plate.js dependencies in `apps/web`: `platejs`, `@platejs/basic-nodes`, `@platejs/markdown`, `@platejs/list`, `@platejs/link`, `@platejs/code-block`, `@platejs/table`, and `remark-gfm` for GFM support.
- Created `apps/web/src/components/plan-editor.tsx` with a full `PlanEditor` component featuring: Plate.js WYSIWYG editor with plugins for headings (h1-h6), blockquote, code block, code line, link, list, table, bold, italic, strikethrough, and inline code; markdown deserialization from `prd.read` RPC on mount; debounced auto-save (1.5s) and on-blur save via `prd.update` RPC; loading spinner, error state with actionable "Go back" button, and save indicator (saved/unsaved/saving) in the editor header bar.
- Updated `apps/web/src/components/plan-list.tsx` to accept optional `selectedPlanId` and `onSelectPlan` callback props, enabling parent-driven plan selection while preserving local fallback state for backward compatibility.
- Updated `apps/web/src/components/project-group.tsx` to pass `selectedPlanId` and `onSelectPlan` through to `PlanList`.
- Updated `apps/web/src/routes/index.tsx` to add `"plan"` as a third `MainView` type alongside `"panels"` and `"dashboard"`, with `selectedPlanId` state and `handleSelectPlan`/`handlePlanBack` callbacks. Selecting a plan in the sidebar switches the main content area to the `PlanEditor`; clicking "Back" returns to the panels view.
- Added `apps/web/test/plan-editor.test.tsx` with 6 tests covering loading state, error state, successful PRD rendering, back navigation from both loaded and error states, and correct `prd.read` RPC invocation with the right prdId.
- Verified with `bun run check-types` and `bun run test` (53 tests, 6 files, all passing) in `apps/web`, and `bun x ultracite check` on all touched files.
- Selected Issue 191 as the highest-priority ready task because it is on the critical path: it unblocks Issue 192 (create workspace from plan), which unblocks Issue 193 (scoped task list), which unblocks Issue 196 (final polish). Issue 194 (removal) is a simpler leaf task that only gates the final polish.
- Created `apps/web/src/components/plan-issues-list.tsx` with a `PlanIssuesList` component that queries LiveStore for tasks with source `"prd"` and matching `prdId`, renders each issue as a card with status icon and inline status dropdown matching the existing `TaskList` styling (same `StatusIcon`, `getStatusClasses`, `STATUS_OPTIONS` patterns), and shows an empty state when no issues exist.
- Updated `apps/web/src/routes/index.tsx` to render the plan detail view as a responsive split layout: the `PlanEditor` fills the left/main area while a fixed-width (w-80) issues sidebar on the right shows a `PlanIssuesList` with its own scrollable area and header. Added the `PlanIssuesList` import.
- Added `apps/web/test/plan-issues-list.test.tsx` with 6 tests covering: filtering to only the matching prdId's PRD-source tasks, empty state rendering, status icon display for all four statuses, preservation of creation order, externalId display when present, and externalId absence when null.
- Verified with `bun run check-types`, `bun run test` (59 tests, 7 files, all passing) in `apps/web`, and `bun x ultracite check src/components/plan-issues-list.tsx src/routes/index.tsx test/plan-issues-list.test.tsx`.
- Selected Issue 192 as the highest-priority ready task because it is on the critical path: it unblocks Issue 193 (plan workspace scoped task list and rlph integration), which unblocks Issue 196 (final polish). Issue 194 (removal) is a simpler leaf task.
- Created `apps/web/src/components/create-plan-workspace.tsx` with a `CreatePlanWorkspace` component featuring: LiveStore-driven PRD lookup and workspace existence detection, branch name derivation from plan slug (`plan/<slug>` convention), disabled state with tooltip when a workspace already exists for the plan, loading/progress state during workspace creation, and error display with toast notifications.
- Updated `apps/web/src/routes/index.tsx` to import and render `CreatePlanWorkspace` in the plan detail view's right sidebar, positioned above the `PlanIssuesList` within the scrollable issues area.
- Association between plan and workspace is by branch-name convention: workspaces with `branchName === "plan/<slug>"` for the same project are considered linked, and destroyed workspaces are excluded from the check.
- Added `apps/web/test/create-plan-workspace.test.tsx` with 8 tests covering: button rendering when no workspace exists, disabled state with tooltip for existing workspace, destroyed-workspace exclusion, correct `workspace.create` RPC call with plan-derived branch name, null render for missing PRD, cross-project workspace isolation, and `planBranchName` utility.
- Verified with `bun run check-types`, `bun run test` (67 tests, 8 files, all passing) in `apps/web`, and `bun x ultracite check src/components/create-plan-workspace.tsx src/routes/index.tsx test/create-plan-workspace.test.tsx`.
- Selected Issue 193 as the highest-priority ready task because it is on the critical path for the final polish issue (196), connects the workspace view to the plan system, and enables rlph integration — the core value proposition of the PRD-driven workflow.
- Updated `apps/web/src/components/workspace-list.tsx` to detect plan-associated workspaces by matching branch names against the `plan/<slug>` convention, querying LiveStore PRDs, and building a branchName-to-prdId lookup map scoped to the current project. Plan-associated workspaces now render a `PlanIssuesList` inside the workspace card under a "Plan Issues" heading, showing only that plan's issues with full status management.
- The scoped task list reuses the existing `PlanIssuesList` component (from Issue 191), which uses the same `StatusIcon`, `getStatusClasses`, status dropdown, and card components as the full `TaskList`, ensuring visual consistency.
- rlph start loop button already works on plan-associated workspaces (unchanged). MCP auto-registration (Issues 187-188) ensures rlph has access to `list_remaining_issues` and `read_prd` MCP tools. Issue status updates propagate to the sidebar plan progress indicator via LiveStore reactivity (both `PlanIssuesList` and `PlanList` query the same tasks table).
- Added `apps/web/test/workspace-plan-scope.test.tsx` with 7 tests covering: plan-associated workspace showing scoped task list, non-plan workspaces not showing plan issues, mixed workspaces with only matching ones showing plan issues, cross-project PRD isolation, empty state when plan has no issues, destroyed workspace exclusion, and rlph start loop button presence.
- Verified with `bun run check-types`, `bun run test` (74 tests, 9 files, all passing) in `apps/web`, and `bun x ultracite check apps/web/src/components/workspace-list.tsx apps/web/test/workspace-plan-scope.test.tsx`.
- Selected Issue 194 as the highest-priority ready task because it is the only remaining Ready task and the last blocker for Issue 196 (final polish and end-to-end verification).
- Deleted `apps/web/src/components/create-task-form.tsx`, removing the manual task creation dialog component entirely.
- Updated `apps/web/src/components/task-source-picker.helpers.ts` to remove `"manual"` from the `TaskSourceFilter` type (now `"linear" | "github"` only) and removed the `isImportSource` helper since all remaining sources are import sources.
- Updated `apps/web/src/components/task-source-picker.tsx` to remove the Manual tab trigger, remove the `CreateTaskForm` import and conditional rendering, and always show the Sync button for the active import source.
- Updated `apps/web/src/components/project-group.tsx` to default `taskSource` state to `"linear"` instead of `"manual"` and updated the comment to reflect the new Linear/GitHub source selection.
- Preserved `"manual"` in the `TaskSource` display type in `task-list.tsx` and `getSourceClasses` so existing manual tasks still render with correct badges — only the creation UI was removed.
- Confirmed `task.create` RPC in `packages/shared/src/rpc.ts` and its handler in `packages/server/src/rpc/handlers.ts` remain untouched (still used by MCP).
- Updated `apps/web/test/task-source-picker.test.ts` to remove the manual-specific test case and adjust sample data to use only Linear and GitHub sources.
- Verified with `bun run check-types` in `apps/web`, `packages/shared`, and `packages/server`; `bun run test` (73 tests, 9 files, all passing) in `apps/web`; and `bun x ultracite check` on all touched files.
- Selected Issue 196 as the highest-priority (and only remaining) ready task — it is the final polish and end-to-end verification pass for the full MCP + PRD-driven task workflow.
- Reviewed all 13 polishing requirements from the PRD against the existing codebase implementation:
  - Plans sub-section visual hierarchy: already consistent with Workspaces and Tasks sections (same `Collapsible`, `text-xs`, `font-medium`, `text-muted-foreground` patterns).
  - Plate.js editor: already loads without layout shift (loading spinner shown while fetching, `initialMarkdown !== null` guard prevents premature editor render).
  - Issue status indicators: `StatusIcon` and `getStatusClasses` in `plan-issues-list.tsx` are identical to `task-list.tsx` (same icons, colors, and badge patterns).
  - Create Workspace button: already disabled with tooltip when workspace exists (`create-plan-workspace.tsx:116-132`).
  - MCP auto-registration: `mergeOpencodeConfig`, `mergeClaudeConfig`, `mergeCodexConfig` all preserve existing user entries by only touching the `laborer` key.
  - MCP concurrent tool calls: tools are stateless RPC delegates with no shared mutable state, handled gracefully by Effect's concurrency model.
  - Error states: plan editor shows `AlertCircle` icon, descriptive message, and "Go back" button; save failures show toast notifications.
  - Keyboard navigation: plan items are `<button type="button">` with `aria-pressed`, collapsible trigger has `focus-visible:ring-2` and `focus-visible:ring-ring`.
  - Real-time updates: `PlanIssuesList` and `PlanList` both query LiveStore reactively via `store.useQuery()`, so status changes propagate automatically.
  - Branch name: `planBranchName` produces `plan/<slug>` which is a sensible, recognizable convention.
  - Markdown round-trip: `MarkdownPlugin` with `remarkGfm` configured for standard GFM serialization/deserialization.
  - No visual regressions: all sidebar sections use consistent `Card`, `Badge`, `Collapsible`, `Empty` component patterns.
- Fixed the plan detail view responsive layout in `apps/web/src/routes/index.tsx`: added `PanelHeaderBar` to the plan view for consistent sidebar toggle and view switching, and made the editor/issues split layout responsive with `flex-col md:flex-row` so the issues sidebar stacks below the editor on narrow viewports (with a fixed `h-64` height) and sits side-by-side at `md:w-80` on wider screens. Added "Plan" label to the header bar context display.
- Verified with `bun run check-types` and `bun run test` (73 tests, 9 files, all passing) in `apps/web`, and `bun x ultracite check apps/web/src/routes/index.tsx`.
