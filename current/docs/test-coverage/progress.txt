2026-03-06: Completed Issue 1 by extracting `packages/server/test/helpers/test-store.ts` and switching every server test with an inline `makeTestStore`/`TestLaborerStore` definition over to the shared helper. Verified the touched tests with `bunx vitest run` and linted the modified files with `bun x ultracite check`; package-wide `bun run test` / `bun run check-types` still fail on the pre-existing `packages/server/src/pty-host.ts` missing `node-pty` dependency/type errors.
2026-03-06: Started Issue 13 because it unlocks the largest set of follow-on RPC coverage tasks. Added reusable in-memory Laborer RPC test infrastructure in `packages/server/test/rpc/test-layer.ts` with stubbed terminal-bound services, plus a tracer-bullet `health.check` test in `packages/server/test/rpc/health-check.test.ts`. Verified the new test with `bunx vitest run test/rpc/health-check.test.ts` and linted the new files with `bun x ultracite check test/rpc/test-layer.ts test/rpc/health-check.test.ts`; package-wide `bun run test` and `bun run check-types` still fail on the pre-existing server `pty-host` / `node-pty` issues.
2026-03-06: Started Issue 2 because it unlocks the remaining worktree/workspace test migrations and several RPC integration tasks. Added `packages/server/test/helpers/git-helpers.ts`, switched the worktree/workspace-oriented server tests over to the shared `createTempDir` / `git` / `initRepo` helpers, and verified the touched files with `bunx vitest run test/worktree-detector.test.ts test/project-registry.worktree-detection.test.ts test/worktree-reconciler.test.ts test/worktree-watcher.test.ts test/workspace-destroy-origin.test.ts test/workspace-validation.test.ts` plus `bun x ultracite check`; `bun run test` and `bun run check-types` in `packages/server` still fail on the pre-existing `packages/server/src/pty-host.ts` / missing `node-pty` issues.
2026-03-06: Completed Issue 4 because it had the highest immediate priority: it removed the dead skipped `packages/server/test/terminal-manager.test.ts` suite and the duplicate `packages/server/test/pty-host.test.ts` / `packages/server/test/ring-buffer.test.ts` files now owned by `packages/terminal`. Verified the cleanup with `bun run test` and `bun x ultracite check test` in `packages/server`, and confirmed there are no remaining `describe.skip` / `it.skip` matches under `packages/server/test`.
2026-03-06: Completed Issue 14 because the shared in-memory Laborer RPC client from Issue 13 was already in place and `project.add` / `project.remove` were the highest-leverage real handler flows to cover next. Added `packages/server/test/rpc/project-management.test.ts` plus a scoped RPC/store helper in `packages/server/test/rpc/test-layer.ts`, then verified project registration, invalid repo rejection, project deletion, and missing-project errors through the RPC contract with real git repos and LiveStore state. Feedback loops: `bunx vitest run test/rpc/health-check.test.ts test/rpc/project-management.test.ts`, `bun run test`, and `bun x ultracite check test/rpc/test-layer.ts test/rpc/health-check.test.ts test/rpc/project-management.test.ts` all pass in `packages/server`; `bun run check-types` still fails on the pre-existing `packages/server/src/pty-host.ts` `node-pty` / implicit-any errors.
2026-03-06: Completed Issue 15 because it closes one of the PRD's explicit mocked-integration gaps and reuses the RPC test harness already built in Issue 13. Replaced the mocked `packages/server/test/rpc-config-handlers.test.ts` coverage with real in-memory RPC integration tests in `packages/server/test/rpc/config-management.test.ts`, verifying `config.get` provenance across ancestor/project config files, missing-project errors, and `config.update` writing/re-reading through real `ConfigService` + `ProjectRegistry` layers. Feedback loops: `bunx vitest run test/rpc/config-management.test.ts`, `bunx vitest run test/rpc/health-check.test.ts test/rpc/project-management.test.ts test/rpc/config-management.test.ts`, `bun run test`, and `bun x ultracite check test/rpc/config-management.test.ts test/rpc/test-layer.ts` all pass in `packages/server`; `bun run check-types` still fails only on the pre-existing `packages/server/src/pty-host.ts` `node-pty` / implicit-any errors.
2026-03-06: Completed Issue 16 because the reusable Laborer RPC harness from Issue 13 already covered the required server layers and `workspace.create` / `workspace.destroy` were the highest-priority uncovered lifecycle endpoints. Added `packages/server/test/rpc/workspace-management.test.ts` to verify worktree creation, setup-script env injection, missing-project errors, laborer-vs-external destroy behavior, and terminal cleanup through the RPC contract; also extended `packages/server/test/rpc/test-layer.ts` with a scoped TerminalClient recorder so the tests can prove the system-boundary stub is used. Feedback loops: `bunx vitest run test/rpc/workspace-management.test.ts`, `bun run test`, and `bun x ultracite check test/rpc/test-layer.ts test/rpc/workspace-management.test.ts` all pass in `packages/server`; `bun run check-types` still fails only on the pre-existing `packages/server/src/pty-host.ts` `node-pty` / implicit-any errors.
2026-03-06: Completed Issue 10 because the shared schema suite still lacked coverage for most active materializers and this closes the remaining data-pipeline gaps before the deprecated terminal no-op checks in Issue 11. Expanded `packages/shared/test/schema.test.ts` with `@effect/vitest` coverage for diff upsert/delete behavior, task lifecycle materialization, and panel layout upsert flows while preserving the PRD lifecycle assertions already moved in Issue 9. Feedback loops: `bun run test`, `bun run check-types`, and `bun x ultracite check test/schema.test.ts` all pass in `packages/shared`.
2026-03-06: Completed Issue 11 because it was the highest-priority remaining shared-schema safety net and closes the backward-compatibility gap around deprecated terminal events before more RPC coverage work. Expanded `packages/shared/test/schema.test.ts` with `@effect/vitest` coverage proving `TerminalSpawned`, `TerminalOutput`, `TerminalStatusChanged`, `TerminalKilled`, `TerminalRemoved`, and `TerminalRestarted` leave every active table unchanged. Feedback loops: `bun run test`, `bun run check-types`, and `bun x ultracite check test/schema.test.ts` all pass in `packages/shared`.
2026-03-06: Completed Issue 3 because it is on the critical path: it unblocks Issue 5 (worktree/workspace @effect/vitest migration), which unblocks Issue 12 (config-service rewrite). Created `packages/server/test/helpers/timing-helpers.ts` exporting `delay` (Promise-based sleep) and `waitFor` (polls a condition with timeout). Updated `packages/server/test/worktree-watcher.test.ts` to import from the shared helper instead of defining inline. The terminal package's `delay` in `terminal-manager.test.ts` was left in place since it belongs to a separate package with its own test infrastructure. Feedback loops: `bunx vitest run test/worktree-watcher.test.ts` (4 tests pass), `bun run test` (22 files, 106 tests pass), and `bun x ultracite check test/helpers/timing-helpers.ts test/worktree-watcher.test.ts` all pass in `packages/server`.
2026-03-06: Completed Issue 5 because it unblocks Issue 12 (config-service rewrite through public API) and is the highest-priority migration task. Migrated all 5 worktree/workspace server test files to `@effect/vitest` patterns: `workspace-destroy-origin.test.ts`, `workspace-validation.test.ts`, `worktree-detector.test.ts`, `worktree-reconciler.test.ts`, and `worktree-watcher.test.ts`. Replaced `import { describe, expect, it } from "vitest"` with `import { assert, describe, it } from "@effect/vitest"`, replaced manual `beforeEach`/`afterEach` scope management + `runEffect` with `it.scoped` + `Effect.provide(TestLayer)`, replaced manual `Effect.runPromise` with `it.effect`, and replaced all `expect().toBe()`/`expect().toContain()`/etc. assertions with `assert.strictEqual`/`assert.include`/`assert.isTrue`/`assert.isFalse`/`assert.isDefined`. Feedback loops: `bunx vitest run` on all 5 files (30 tests pass), `bun run test` (22 files, 106 tests pass), and `bun x ultracite check` on all 5 files passes cleanly in `packages/server`.
2026-03-06: Completed Issue 8 because it was the highest-priority remaining task and it unblocks Issue 21 (the last remaining task). Migrated `packages/terminal/test/terminal-manager.test.ts` to use `assert` from `@effect/vitest` instead of `expect` from vitest. Replaced `import { afterAll, beforeAll, describe, expect, it } from "vitest"` with `import { assert, describe } from "@effect/vitest"` plus `import { afterAll, beforeAll, it } from "vitest"` to preserve the custom `runEffect` / `beforeAll` / `afterAll` shared-scope pattern. Replaced all 28 `expect()` calls with `assert.*` equivalents: `expect(x).toBeDefined()` to `assert.isDefined(x)`, `expect(x).toBeUndefined()` to `assert.isUndefined(x)`, `expect(x).toBe(y)` to `assert.strictEqual(x, y)`, `expect(x).toEqual(y)` to `assert.deepStrictEqual(x, y)`, and `expect(x).toBe(false)` to `assert.isFalse(x)`. Feedback loops: `bun run test` (3 files, 59 tests pass), `bun run check-types` passes cleanly, and `bun x ultracite check test/terminal-manager.test.ts` passes with no fixes needed in `packages/terminal`.
2026-03-06: Completed Issue 21 because it was the last remaining task in `docs/test-coverage/issues-test-coverage.md`. Added `packages/terminal/test/rpc-integration.test.ts` with in-memory RPC integration tests for all 8 TerminalRpcs endpoints using `RpcTest.makeClient(TerminalRpcs)` backed by real `TerminalManager.layer` + `PtyHostClient.layer`. Tests cover: `terminal.spawn` (basic + args), `terminal.write` (success + nonexistent error), `terminal.resize` (success + nonexistent error), `terminal.kill` (success + nonexistent error), `terminal.remove` (success + nonexistent error), `terminal.restart` (success + nonexistent error), `terminal.list` (running + stopped), and `terminal.events` (streaming lifecycle events via `Stream.fromPubSub`). Uses the same shared-scope `beforeAll`/`afterAll` pattern as `terminal-manager.test.ts` for the long-lived PtyHostClient layer. Feedback loops: `bunx vitest run test/rpc-integration.test.ts` (14 tests pass), `bun run test` (4 files, 73 tests pass), `bun run check-types` passes cleanly, and `bun x ultracite check test/rpc-integration.test.ts` passes with no fixes needed in `packages/terminal`.
