## Issue 1: Positional metadata on commands (OSC 633;A + 633;C) — COMPLETED

### Summary
Added OSC 633;A (prompt start) and 633;C (command executed) handlers to the
headless terminal's shell integration, and enhanced 633;B and 633;D to track
positional metadata via xterm buffer positions. Serialized commands now include
`promptStartLine`, `startLine`, `executedLine`, `endLine`, `startX`, `executedX`,
and `commandStartLineContent` fields populated from live command tracking.

### Changes
- `packages/terminal/src/lib/headless-terminal.ts`:
  - Added `getBufferLineContent()` and `getAbsoluteCursorLine()` helpers
  - Added OSC 633;A handler: records prompt start line in `pendingPromptStartLine`
  - Enhanced OSC 633;B handler: registers cursor line as `startLine`, captures
    `startX` from `cursorX`, captures `commandStartLineContent` from buffer,
    consumes `pendingPromptStartLine` as `promptStartLine`
  - Added OSC 633;C handler: records `executedLine` and `executedX` on in-flight command
  - Enhanced OSC 633;D handler: records `endLine` from cursor position, pushes
    completed command with all positional fields
  - Extended `InFlightCommand` interface with positional fields
  - Added `pendingPromptStartLine` to `HeadlessCommandState`
  - Updated `serializeCurrentCommand()` to include positional metadata for in-flight commands
  - Extracted `handleCommandFinished()` and `handlePropertySequence()` helper
    functions to keep complexity under Biome's limit

- `packages/terminal/test/headless-terminal.test.ts`:
  - Updated 5 existing tests to expect positional metadata fields
  - Added 7 new tests:
    - Full A->B->E->C->D lifecycle with positional ordering assertions
    - `commandStartLineContent` capture from buffer at 633;B
    - `startX` from cursor position at 633;B
    - Multiple commands with ordering assertions
    - 633;B without preceding 633;A (promptStartLine undefined)
    - 633;D without preceding 633;C (executedLine/executedX undefined)
    - In-flight command after A->B includes positional metadata but not endLine

- `packages/terminal/test/terminal-session-persistence.test.ts`:
  - Updated 1 test to expect positional metadata in persisted state

### Verification
- TypeScript: `npx tsc --noEmit` passes with 0 errors
- Biome: `bunx biome check` passes on all modified files
- Tests: 25/25 headless-terminal tests pass, 32/32 persistence tests pass
- Data channel tests: pre-existing flaky failures confirmed unchanged
- Terminal-manager tests: pre-existing infrastructure failure confirmed unchanged

### Unblocks
- Issue 2: Complete PromptInputModel
- Issue 3: FinalTerm 133 command detection fallback
- Issue 4: OSC 633;F/G/H/I (continuation + right prompt)
- Issue 9: Renderer shell integration addon + command deserialization

## Issue 5: Capability store + CwdDetection — COMPLETED

### Summary
Created the capability store infrastructure and implemented CwdDetection as the
first capability. The capability store tracks per-terminal capabilities that are
serializable alongside command detection. CwdDetection is fed by four OSC sources:
OSC 633;P Cwd (VS Code), OSC 7 (standard file:// URI), OSC 9 (Windows-friendly),
and OSC 1337 CurrentDir (iTerm2). Capabilities flow through all layers: headless
terminal tracking, persistence serialization, data channel replay, and the
terminal manager.

### Changes
- `packages/terminal/src/services/terminal-session-persistence.ts`:
  - Added `SerializedCwdDetectionEntry`, `SerializedCwdDetection`, and
    `SerializedCapabilityStore` types
  - Added optional `capabilities` field to `SerializedReplayEvent`
  - Updated `ReplayRecorder` to track and roundtrip `capabilities`
  - Updated `serializeState()` to accept and persist `getCapabilityState` getter
  - Exported all new types

- `packages/terminal/src/lib/headless-terminal.ts`:
  - Added `CwdDetectionState` interface for per-terminal cwd tracking
  - Added `cwdDetection` field to `HeadlessCommandState`
  - Added `recordCwdChange()` helper that feeds both legacy `commandState.cwd`
    and the new `cwdDetection` capability state
  - Added `parseFileUri()` helper for OSC 7 file:// URI parsing with
    percent-decoding
  - Updated OSC 633;P Cwd handler to use `recordCwdChange()` instead of
    direct assignment
  - Added OSC 7 handler: parses file:// URIs and feeds CwdDetection
  - Added OSC 9 handler: parses Windows sub-command 9 format and feeds CwdDetection
  - Added OSC 1337 handler: parses CurrentDir= payload and feeds CwdDetection
  - Added `getCapabilityState()` method to `HeadlessTerminalManager` interface
    and implementation, which serializes the cwd detection state

- `packages/terminal/src/services/terminal-data-channel.ts`:
  - Updated `encodeReplay()` to include `capabilities` field in JSON payload

- `packages/terminal/src/services/terminal-manager.ts`:
  - Added `getCapabilityState()` to the `TerminalManager` interface
  - Wired through to `headlessManager.getCapabilityState()` in implementation

- `packages/terminal/src/utility-main.ts`:
  - Updated `serializeState()` call to pass `getCapabilityState` getter

- `packages/terminal/test/headless-terminal.test.ts`:
  - Added 13 new tests:
    - CwdDetection from OSC 633;P Cwd
    - CwdDetection from OSC 7 with hostname
    - CwdDetection from OSC 7 with empty hostname
    - CwdDetection from OSC 7 with percent-encoded characters
    - CwdDetection from OSC 9;9 (Windows)
    - CwdDetection from OSC 1337 CurrentDir
    - Cwd history across multiple changes from different sources
    - Cwd detection feeding into command cwd field
    - Returns undefined when no cwd detected
    - Returns undefined for non-existent terminal
    - Ignores OSC 7 with non-file URI
    - Ignores OSC 9 without sub-command 9 prefix
    - Ignores OSC 1337 with non-CurrentDir payload

- `packages/terminal/test/terminal-session-persistence.test.ts`:
  - Added 4 new tests:
    - Serializes live capability state from headless terminal cwd detection
    - Preserves restored capability state when serializing again
    - Serializes cwd detection from multiple OSC sources
    - Omits capabilities when no capability state exists

### Verification
- TypeScript: `npx tsc --noEmit` passes with 0 errors
- Biome: `bunx biome check` passes on all modified files
- Tests: 38/38 headless-terminal tests pass, 36/36 persistence tests pass
- All 208 tests across 10 test files pass

### Unblocks
- Issue 6: BufferMarkDetection + gutter UI
- Issue 7: ShellEnvDetection + indicator
- Issue 8: PromptTypeDetection + badge

## Issue 10: Replay input guard — COMPLETED

### Summary
Added replay input guards to both the server-side data channel and the
client-side renderer to prevent user input, resize events, and flow control
ack messages from corrupting terminal state during replay. VS Code implements
this as `_inReplay = true` on both sides — we now have equivalent protection.

The guard operates at three layers:
1. **Server-side (data channel)**: An `isReplayingToRenderer` flag is set when
   the replay payload is sent and cleared after `replayComplete`. During this
   window, all incoming renderer messages (input keystrokes, ack messages) are
   dropped by the `messageHandler`.
2. **Client-side (send guard)**: The `send()` function in
   `useTerminalMessagePort` checks a `replayStatusRef` and no-ops when
   `replaying`. Flow control acks are also suppressed during replay.
3. **Client-side (resize guard)**: The `ResizeObserver` callback in
   `TerminalPaneRenderer` checks a `replayStatusRef` and skips resize
   processing during replay. The post-replay fit (already guarded by
   `replayStatus !== 'complete'`) handles the initial resize after replay.

### Changes
- `packages/terminal/src/services/terminal-data-channel.ts`:
  - Added `isReplayingToRenderer` flag, set to `true` before `encodeReplay()`
    and cleared after `encodeReplayComplete()`
  - Added early return in `messageHandler` when `isReplayingToRenderer` is true,
    dropping all incoming renderer messages during the replay window

- `apps/web/src/hooks/use-terminal-messageport.ts`:
  - Added `replayStatusRef` to track replay status for memoized callbacks
  - `send()` no-ops when `replayStatusRef.current === 'replaying'`
  - `maybeAck()` suppresses ack messages during replay
  - `handleControlMessage` syncs `replayStatusRef` alongside React state
  - Effect cleanup resets `replayStatusRef` on remount

- `apps/web/src/panes/terminal-pane.tsx`:
  - Added `replayStatusRef` in `TerminalPaneRenderer` synced from props
  - `ResizeObserver` callback skips resize when `replayStatusRef.current ===
    'replaying'`

- `packages/terminal/test/terminal-data-channel.test.ts`:
  - Added test: replay and replayComplete sent in correct order with input
    guard infrastructure in place, input works after replay completes
  - Added test: input resumes correctly after replayComplete

- `apps/web/test/use-terminal-messageport.test.ts`:
  - Added test: `send()` no-ops during replay and resumes after replayComplete

### Verification
- TypeScript: `npx tsc --noEmit -p packages/terminal/tsconfig.json` passes
- TypeScript: `npx tsc --noEmit -p apps/web/tsconfig.json` passes
- Biome: `bunx biome check` passes on all 5 modified files
- Tests: 12/12 data channel tests pass (2 new)
- Tests: 2/2 messageport hook tests pass (1 new)

## Issue 11: rawReviveBuffer optimization — COMPLETED

### Summary
Implemented the rawReviveBuffer optimization to avoid cumulative data loss from
repeated serialize/deserialize cycles on idle terminals. When a terminal is
revived from persisted state, the original raw screen state is stored alongside
the headless xterm instance. If the terminal remains idle (no user input or
renderer-initiated resize), `getScreenState()` returns the stored raw buffer
instead of re-serializing through the xterm serialize addon. This bounds
serialization degradation to one cycle per user interaction epoch.

The optimization tracks per-terminal interaction state through three phases:
- `'none'` — Terminal freshly created or recovered, no interaction yet
- `'replay-only'` — Revived replay sent to renderer, but user never typed/resized
- `'session'` — User has directly interacted; raw buffer freed, live serialize used

### Changes
- `packages/terminal/src/lib/headless-terminal.ts`:
  - Added `TerminalInteractionState` type (`'none' | 'replay-only' | 'session'`)
  - Extended `HeadlessTerminalState` with `interactionState` and `rawReviveBuffer`
  - Updated `getScreenState()` to return raw buffer for idle terminals
  - Added `setRawReviveBuffer()` — stores raw buffer on terminal revival
  - Added `freeRawReviveBuffer()` — frees buffer and transitions to `'session'`
  - Added `markReplayed()` — transitions from `'none'` to `'replay-only'`
  - Updated `HeadlessTerminalManager` interface with three new methods

- `packages/terminal/src/services/terminal-manager.ts`:
  - Added `freeRawReviveBuffer`, `setRawReviveBuffer`, `markReplayed` to
    `TerminalManager` interface and wired through to headless manager
  - Added `freeRawReviveBuffer()` call in `write` handler (user input)
  - Added `freeRawReviveBuffer()` call in `resize` handler (renderer resize)

- `packages/terminal/src/services/terminal-data-channel.ts`:
  - Added `markReplayed()` call after sending revived replay event to renderer

- `packages/terminal/src/utility-main.ts`:
  - Added `setRawReviveBuffer()` call during terminal restoration with the
    persisted `screenState` value

- `packages/terminal/test/headless-terminal.test.ts`:
  - Added 10 new tests:
    - Raw buffer returned for idle terminal (none state)
    - Raw buffer returned for replay-only state
    - Live xterm state returned after freeRawReviveBuffer
    - freeRawReviveBuffer transitions state to session
    - markReplayed only transitions from none to replay-only
    - freeRawReviveBuffer no-op for non-existent terminal
    - setRawReviveBuffer no-op for non-existent terminal
    - markReplayed no-op for non-existent terminal
    - Resize + free raw buffer combination
    - Without raw buffer, getScreenState uses serialize addon

- `packages/terminal/test/terminal-session-persistence.test.ts`:
  - Added 3 new tests:
    - Serializes raw revive buffer for idle terminal
    - Serializes live xterm state after raw buffer is freed
    - Raw revive buffer survives replay-only transition

### Verification
- TypeScript: `npx tsc --noEmit -p packages/terminal/tsconfig.json` passes
- TypeScript: `npx tsc --noEmit -p apps/web/tsconfig.json` passes
- Biome: `bunx biome check` passes on all 6 modified files
- Tests: 48/48 headless-terminal tests pass (10 new)
- Tests: 39/39 persistence tests pass (3 new)
- All 125 tests across test files pass (pre-existing empty test suite failures unchanged)
