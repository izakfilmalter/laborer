# Progress: Migrate from Tauri to Electron (and Bun to Node.js)

## Status: In Progress

## Completed

### Issue 1: Node.js spawn utility — DONE
- Created `packages/server/src/lib/spawn.ts` — a `child_process`-based spawn wrapper with Bun.spawn()-compatible ergonomics
- Returns Web ReadableStreams for stdout/stderr so existing `new Response(proc.stdout).text()` pattern works unchanged
- Supports all usage patterns found in the codebase:
  - Simple command execution with exit code
  - stdout/stderr capture via ReadableStream
  - Parallel stdout+stderr reads with Promise.all
  - Process-to-process piping (stdin: anotherProc.stdout)
  - stdout: 'ignore' mode
  - Custom cwd and env options
  - Process killing
- Created `packages/server/test/spawn.test.ts` with 16 unit tests covering all patterns
- All tests passing, TypeScript compiles, formatting clean

### Issue 2: Migrate workspace-provider Bun.spawn sites — DONE
- Replaced all 16 `Bun.spawn()` call sites in `packages/server/src/services/workspace-provider.ts` with the `spawn()` utility from `packages/server/src/lib/spawn.ts`
- Added `import { spawn } from '../lib/spawn.js'` import
- Updated comment referencing Bun.spawn to reflect the new spawn utility
- All `await proc.exited` and `new Response(proc.stdout).text()` patterns unchanged (spawn utility returns compatible Web ReadableStreams)
- No references to `Bun` namespace remain in the file
- TypeScript compiles without errors
- All 279 tests pass (1 pre-existing timeout in deps-image-cache.e2e.test.ts unrelated to this change)
- Formatting clean (no fixes needed)

### Issue 3: Migrate remaining server Bun.spawn sites — DONE
- Replaced all `Bun.spawn()` calls across 6 service files with the `spawn()` utility:
  - `container-service.ts` — 5 calls (docker run/stop/rm/pause/unpause)
  - `deps-image-service.ts` — 10 calls (docker run/rm/cp/commit/exec/inspect + tar pipe pattern)
  - `docker-detection.ts` — 2 calls (which docker, docker info)
  - `diff-service.ts` — 1 call (git diff via spawnGit helper)
  - `pr-watcher.ts` — 1 call (gh pr view)
  - `handlers.ts` — 2 calls (gh pr view, editor open)
- The tar pipe pattern in `deps-image-service.ts` (piping stdout from docker exec tar into tar extract stdin) works correctly using the spawn utility's Web ReadableStream stdin support
- Removed all `ensureBunSpawnForNodeTests()` polyfills from 5 test files:
  - `docker-detection.e2e.test.ts`
  - `container-service.e2e.test.ts`
  - `deps-image-cache.e2e.test.ts`
  - `workspace-destroy-origin.test.ts`
  - `rpc/workspace-management.test.ts`
- Removed now-unused imports (`spawn` from `node:child_process`, `Readable` from `node:stream`, `beforeAll` from `vitest`) from test files
- Updated outdated comment in `diff-service-event-consumer.test.ts` referencing Bun.spawn
- No references to `Bun` namespace remain in any service file or test file (only in comments in spawn.ts and pty-host-client.ts)
- TypeScript compiles without errors
- All 279 tests pass (1 pre-existing cache-hit flake in deps-image-cache.e2e.test.ts unrelated to this change)
- Formatting clean (no fixes needed)

### Issue 4: Server Bun-to-Node runtime swap — DONE
- Replaced `@effect/platform-bun` with `@effect/platform-node` in `packages/server/src/main.ts`:
  - `BunHttpServer.layer({ port })` → `NodeHttpServer.layer(createServer, { port })`
  - `BunRuntime.runMain(main)` → `NodeRuntime.runMain(main)`
  - Added `import { createServer } from 'node:http'`
  - Updated comments from "Bun server" to "Node.js server"
- Replaced `bun:sqlite` with `better-sqlite3` in `packages/server/src/services/sync-backend.ts`:
  - `import { Database } from 'bun:sqlite'` → `import Database from 'better-sqlite3'`
  - `new Database(path, { create: true })` → `new Database(path)` (better-sqlite3 creates by default)
  - `db.exec('PRAGMA ...')` → `db.pragma('...')` (idiomatic better-sqlite3 API)
  - `db.query(...)` → `db.prepare(...)` (all 8 prepared statement sites)
  - `contextRow === null` → `contextRow === undefined` (better-sqlite3 returns undefined for no match)
  - All typed generics translated: `db.query<Result, Params>()` → `db.prepare<Params, Result>()` (param order swapped)
  - WAL mode, prepared statements, transaction-wrapped batch inserts all preserved
- Updated `packages/server/tsconfig.json`: removed `"bun"` from types array (now `["node"]` only)
- Updated `packages/server/package.json`:
  - Swapped `@effect/platform-bun` → `@effect/platform-node` in dependencies
  - Added `better-sqlite3` (^12.6.2) to dependencies
  - Removed `@types/bun` from devDependencies
  - Added `@types/better-sqlite3` (^7.6.13) to devDependencies
  - Added `tsx` (^4.19.4) to devDependencies
  - Updated scripts: `bun run --watch src/main.ts` → `tsx --watch src/main.ts`, `bun run src/main.ts` → `tsx src/main.ts`
- No root package.json changes needed (`@effect/platform-node` already in catalog at 0.103.0)
- TypeScript compiles without errors
- All 280 tests pass
- Formatting clean

### Issue 5: Terminal + MCP Bun-to-Node runtime swap — DONE
- Replaced `@effect/platform-bun` with `@effect/platform-node` in `packages/terminal/src/main.ts`:
  - `BunHttpServer.layer({ port })` → `NodeHttpServer.layer(createServer, { port })`
  - `BunRuntime.runMain(main)` → `NodeRuntime.runMain(main)`
  - Added `import { createServer } from 'node:http'`
  - Updated all comments from "Bun server" to "Node.js server"
- Replaced `@effect/platform-bun` with `@effect/platform-node` in `packages/mcp/src/main.ts`:
  - `BunRuntime` → `NodeRuntime`, `BunStream.stdin` → `NodeStream.stdin`, `BunSink.stdout` → `NodeSink.stdout`
- Updated both `packages/terminal/tsconfig.json` and `packages/mcp/tsconfig.json`: removed `"bun"` from types array (now `["node"]` only)
- Updated both package.json files:
  - Swapped `@effect/platform-bun` → `@effect/platform-node` in dependencies
  - Removed `@types/bun` from devDependencies
  - Added `tsx` (^4.19.4) to devDependencies
  - Updated scripts: `bun run --watch src/main.ts` → `tsx --watch src/main.ts`, `bun run src/main.ts` → `tsx src/main.ts`
- Updated outdated Bun references in comments:
  - `packages/terminal/src/pty-host.ts` — removed Bun-specific explanation
  - `packages/terminal/src/services/pty-host-client.ts` — simplified comment about PTY Host spawning
- No references to `Bun` namespace remain in either package (source files, package.json, tsconfig)
- TypeScript compiles without errors in both packages
- All 73 terminal tests pass (pty-host, terminal-manager, rpc-integration, ring-buffer)
- All 19 MCP tests pass (project-discovery, issue-tools, prd-tools)
- All 280 server tests still pass (no regressions)
- Formatting clean

### Issue 6: Electron shell scaffold + dev tooling — DONE
- Created `apps/desktop/` package with full Electron shell scaffold:
  - `package.json` with electron 40.6.0, tsdown, wait-on, vitest deps
  - `tsconfig.json` extending @laborer/config base with node types, DOM lib, ES2023
  - `tsdown.config.ts` bundling main.ts + preload.ts to CJS in dist-electron/
- Created `apps/desktop/src/main.ts` — minimal Electron main process:
  - BrowserWindow with hiddenInset title bar, traffic light positioning at (16, 12)
  - contextIsolation: true, nodeIntegration: false, sandbox: true
  - Loads VITE_DEV_SERVER_URL in development (about:blank placeholder in production)
  - macOS lifecycle handling (stay active until explicit quit, activate re-creates window)
  - Preload script path resolved via __dirname in CJS output
- Created `apps/desktop/src/preload.ts` — minimal stub exposing empty desktopBridge
- Created dev scripts following t3code patterns:
  - `scripts/electron-launcher.mjs` — resolves Electron binary, on macOS creates patched .app bundle with "Laborer (Dev)" display name and custom icon
  - `scripts/dev-electron.mjs` — waits for Vite dev server + bundled files, launches Electron, watches dist-electron/ for changes and restarts with 120ms debounce, graceful SIGTERM→SIGKILL shutdown
- Copied icon resources (icon.icns, icon.png) from src-tauri/icons/ to apps/desktop/resources/
- Added .gitignore for dist-electron/ and .electron-runtime/
- Updated turbo.json: added dist-electron/** to build outputs
- `bun install` — 186 packages installed (Electron + tsdown + wait-on)
- `tsdown` build produces main.js (1.45 KB) + preload.js (0.32 KB) in dist-electron/
- TypeScript compiles without errors across all 6 packages
- All 280 server + 73 terminal + 19 MCP tests pass (no regressions)
- Formatting clean (0 fixes needed)

## Now Unblocked
- Issue 7: Shell environment probing + port allocation (blocker Issue 6 done)
- Issue 8: Spawn sidecars as child processes (all Bun-to-Node blockers resolved: Issues 4, 5 done; Issue 7 now ready)
- Issue 14: Window state persistence + application menu (blocker Issue 6 done)
- Issue 15: Auto-update system (blocker Issue 6 done)
- Issue 16: Service bundling for distribution (both blockers resolved: Issues 4, 5 done)

## Ready to start (no blockers):
- Issue 7: Shell environment probing + port allocation
- Issue 14: Window state persistence + application menu
- Issue 15: Auto-update system
- Issue 16: Service bundling for distribution
