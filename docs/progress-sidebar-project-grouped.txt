# Sidebar Project-Grouped Layout — Progress

## Session Log

### Issue 168: ProjectGroup collapsible headings with nested workspaces — DONE

Replaced the flat sidebar layout (separate Projects section + Workspaces section) with a project-grouped tree structure.

Changes made:
- Created `apps/web/src/hooks/use-project-collapse-state.ts` — hook to manage per-project expand/collapse state, persisted to localStorage. Defaults to all projects expanded when no stored state exists.
- Created `apps/web/src/components/project-group.tsx` — new ProjectGroup component that renders each project as a collapsible heading (chevron + folder icon + project name) with project settings and delete actions, and its workspaces nested underneath via the existing WorkspaceList component.
- Modified `apps/web/src/components/workspace-list.tsx`:
  - Changed `WorkspaceListProps` from optional `activeProjectId` to required `projectId` prop.
  - Removed project name from WorkspaceItem cards (CardDescription no longer shows `projectName` since the project context is provided by the parent ProjectGroup heading).
  - Removed unused `allProjects$` query and `projects` schema import.
  - Simplified empty state to a compact text line ("No workspaces") instead of a full Empty component, since each project group already provides context.
- Modified `apps/web/src/routes/index.tsx` (HomeComponent):
  - Replaced flat `<ProjectList>` and `<WorkspaceList>` sections with a loop over projects rendering `<ProjectGroup>` per project.
  - Added `useProjectCollapseState` hook for managing collapse state.
  - Imported `ProjectGroup` component.
  - Removed `ProjectList`, `CreateWorkspaceForm`, and `WorkspaceList` imports (no longer used directly).
  - Kept ProjectSwitcher, Tasks section, and Server Status section temporarily (to be addressed in issues 169-172).

TypeScript: 0 errors. Ultracite: 0 issues.

### Issue 172: Server Status sticky footer — DONE

Extracted the Server Status section from inside the ScrollArea and rendered it as a sticky footer pinned to the bottom of the sidebar panel, always visible regardless of scroll position.

Changes made:
- Modified `apps/web/src/routes/index.tsx` (HomeComponent sidebar):
  - Wrapped the sidebar content in a `div.flex.h-full.flex-col` container inside the ResizablePanel so that the ScrollArea and footer share the panel height via flexbox.
  - Changed the ScrollArea from `className="h-full"` to `className="min-h-0 flex-1"` so it fills remaining space above the footer and allows proper flex shrinking.
  - Moved the Server Status `<section>` out of the ScrollArea's grid and placed it after the ScrollArea as a sibling element with `className="shrink-0 border-t p-3"`.
  - Changed the Server Status styling from `rounded-lg border p-3` (card-style inside scroll) to `shrink-0 border-t p-3` (footer-style with top border separator, no shrinking).

TypeScript: 0 errors. Ultracite: 0 issues.

### Issue 170: Tasks nested under each project — DONE

Moved tasks from the flat bottom section of the sidebar into each ProjectGroup as a sub-section below the project's workspaces. Task source selection is now independent per project.

Changes made:
- Modified `apps/web/src/components/project-group.tsx`:
  - Added local `taskSource` state (`TaskSourceFilter`) for per-project independent task source selection (Manual/Linear/GitHub).
  - Added `TaskSourcePicker` and `TaskList` inside the collapsible content, below the WorkspaceList, separated by a `Separator`.
  - Added a "Tasks" sub-heading to visually distinguish the tasks section from workspaces.
  - Added imports for `TaskList`, `TaskSourcePicker`, `TaskSourceFilter`, and `Separator`.
  - Updated doc comment to reference Issue #170.
- Modified `apps/web/src/components/task-source-picker.tsx`:
  - Changed `activeProjectId?: string | null` prop to required `projectId: string` since tasks are always rendered within a project context.
  - Renamed all `activeProjectId` references to `projectId` throughout.
  - Removed the "Select a project above" conditional message (no longer relevant since project is always known).
  - Simplified the Sync button's disabled state (removed `!projectId` check since it's always a string).
- Modified `apps/web/src/components/task-list.tsx`:
  - Changed `activeProjectId?: string | null` prop to required `projectId: string`.
  - Removed `projectName` prop from `TaskItemProps` and `CardDescription` from task cards (redundant — project context is provided by parent ProjectGroup heading).
  - Removed `allProjects$` query and `projects` schema import (no longer needed for project name lookup).
  - Removed `projectNameMap` construction and usage.
  - Updated doc comment to reference Issue #170.
- Modified `apps/web/src/routes/index.tsx` (HomeComponent):
  - Removed the standalone `<section>` containing Tasks heading, `<TaskSourcePicker>`, and `<TaskList>`.
  - Removed `activeTaskSource` / `setActiveTaskSource` state (no longer global — now per-project inside each ProjectGroup).
  - Removed `TaskList`, `TaskSourcePicker`, and `TaskSourceFilter` imports.
  - `activeProjectId` / `setActiveProjectId` state and `ProjectSwitcher` remain (to be removed in Issue #171).

TypeScript: 0 errors. Ultracite: 0 issues. Tests: 4/4 pass (task-source-picker helpers).
