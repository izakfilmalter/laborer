#!/bin/sh
set -eu

CONTENTS="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd -P)"
DAEMON="$CONTENTS/Resources/daemon"

export NODE_ENV=production
cd "$DAEMON/app"
exec "$DAEMON/bin/node" "$DAEMON/app/src/acp-runtime/production-live.ts"
